# Trace-Frame Decimated Period Certificate Target

Date: 2026-06-06

This note makes the current trace-frame theorem completely explicit for the
p24 arithmetic.  It is the same theorem as:

```text
p24/trace_frame_split_frontier.md
p24/tensor_factor_trace_annihilator_theorem.md
p24/tensor_factor_trace_period_identity.md
p24/trace_frame_relative_trace_normality_lemma.md
```

but stated in the 31-term decimated period coordinates that a proof or finite
certificate would actually touch.

## Exact p24 Schedule

The accounting script is:

```text
p24/tensor_factor_trace_period_identity.py
```

It reports:

```text
p = 1000000000000000000000007
m = 66254 = 2*157*211
n = 3107441
ord_m(p) = 5460
ord_n(p) = 388430
tensor factors over E=F_p(mu_m): 70
one tensor factor degree over E: 5549 = 31*179
```

For one factor, let:

```text
E = F_p(mu_m)
B/E has degree 5549
C/E has degree 179
B/C has degree 31
theta = selected n-th root coordinate in B
```

The `E`-Frobenius multiplier on the chosen factor is:

```text
a = p^5460 mod n = 209035
ord_n(a) = 5549.
```

The trace subgroup for `Tr_{B/C}` is generated by:

```text
h_31 = a^179 mod n = 1293662,
ord_n(h_31) = 31.
```

The first subgroup exponents are:

```text
1, 1293662, 2408079, 2725829, 2208526, 2361259,
603520, 124108, 1649349, 622916, 2513626, 803962, ...
```

The first `C/E` quotient representatives are:

```text
1, 209035, 1903324, 123905, 3068381, 1447848,
1690485, 1663778, 3037510, 2483320, 2777150, 1852394, ...
```

The 179 cosets of `<h_31>` partition the selected 5549-element factor orbit.
The companion normality lemma records that if the selected `n`-root is normal
in this degree-5549 factor over `E`, then these 31-term trace periods form a
normal basis of `C/E`; the rank theorem below is therefore about the CM
axis image, not about a defective trace-coordinate basis.

## Decimated Trace Coordinates

For an axis character frequency `s` in:

```text
S_axis = {0} union S_2 union S_157 union S_211,
|S_axis| = 368,
```

write `R_s(X)` for the `K`-character resolvent of the packet fiber polynomial
evaluated in the chosen tensor factor.

Concretely, with `m=66254`, the component blocks are:

```text
S_2   = {33127}
S_157 = {422*j mod m : 1 <= j < 157}
S_211 = {314*j mod m : 1 <= j < 211}
```

These are the nontrivial character frequencies of the CRT factors
`2`, `157`, and `211`, lifted to characters modulo `m`.

For `i=0,1,2`, define:

```text
T_i(R_s) = Tr_{B/C}(theta^i R_s(theta))
         = sum_{j=0}^{30}
             theta^(i*h_31^j) R_s(theta^(h_31^j)).
```

The full `E`-coordinate set used by the certificate is the 179 Frobenius
conjugates over `E`:

```text
T_{i,t}(R_s)
  = sum_{j=0}^{30}
      theta^(i*a^t*h_31^j) R_s(theta^(a^t*h_31^j)),
    0 <= i < 3, 0 <= t < 179.
```

Thus the theorem is not a degree-388430 object anymore.  It is a `368 x 537`
matrix over `E` whose columns are exactly these decimated period traces.

## Equivalent Failure Statement

Let an axis weight be:

```text
w(r) = alpha
     + lambda_2(r mod 2)
     + lambda_157(r mod 157)
     + lambda_211(r mod 211),
```

with the usual trace-zero normalization on the three nonconstant components,
and define:

```text
x_w = sum_r w(r) J_r(theta) in B.
```

A bad p24 packet exists if and only if there is a nonzero axis weight `w`
such that:

```text
Tr_{B/C}(theta^i x_w) = 0,  i=0,1,2.
```

Equivalently, with `g` the minimal polynomial of `theta` over `C`,

```text
g'(theta) x_w
```

has zero `theta^30`, `theta^29`, and `theta^28` coefficients in the
`C`-basis `1,theta,...,theta^30`.

Equivalently again:

```text
W_axis(B) cap span_C{1,theta,theta^2}^perp = {0}
```

for the trace pairing `Tr_{B/C}(xy)`.

## The Missing Theorem

The open arithmetic theorem can now be phrased without class-set language:

```text
For each of the eight H-character packets and either tensor-factor
representative, the 31-term decimated period trace matrix

  (T_{i,t}(R_s))_{s in S_axis, 0<=i<3, 0<=t<179}

has E-rank 368.
```

By semilinear Frobenius, one tensor factor has full rank if and only if all
70 factors do.  By the scalar-extension and axis gates, this implies the
base-field packet axis injectivity theorem and therefore the current `L1`
packet certificate route.

The current sharp proof-facing version is the factorized Schubert form:

```text
p24/trace_frame_factorized_decimated_period_theorem.md
```

It keeps the same decimated periods but selects the leading coordinate:

```text
Top_3,lead = 179 coordinates from T_0
           + 179 coordinates from T_1
           + 10 coordinates from T_2.
```

The required rank is split into four p-unit factors:

```text
Delta_A, Delta_B, Delta_AB, Delta_tail.
```

Here `Delta_AB` proves the forced `10`-dimensional intersection of the
`A=constant+2+157` and `B=211` prefix images, and `Delta_tail` proves the
third trace coordinate separates that forced kernel.  This is currently the
smallest explicit certificate surface for the p24 decimated trace-frame
route.

## Proof Pressure

This statement is now small enough to be attacked in three non-computational
ways:

```text
1. p-unit Plucker theorem:
   prove one 368-by-368 Plucker coordinate of the above matrix is a p-unit.

2. relative low-degree exclusion:
   prove no nonzero CRT-axis class period can land in the degree <=27
   relative-infinity flag after multiplication by g'(theta).

3. hidden MSRD/LRS equivalence:
   prove the relative coefficient code W_axis(B) is p-unit block-equivalent
   to a high-distance sum-rank code.
```

The existing small CM audits support the rank profile but do not prove it.
The useful next computation is therefore only fast falsification of proposed
identities for these exact decimated traces; the theorem itself must supply a
p-unit or finite-field identity for the selected CM packet.
