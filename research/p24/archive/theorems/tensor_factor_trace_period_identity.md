# Tensor Factor Trace Period Identity

This note unwraps the twisted trace-frame target into explicit p24 period
sums.

## Frobenius Multipliers

The accounting script is:

```text
p24/tensor_factor_trace_period_identity.py
```

It computes:

```text
E = F_p(mu_m),             [E:F_p] = ord_m(p) = 5460
B/E tensor factor degree: 5549
C/E subfield degree:      179
[B:C] = 31
a = p^5460 mod n = 209035
a^179 mod n = 1293662
```

Let:

```text
a = p^5460 mod n.
```

Then `a` has order `5549` modulo `n`, and the trace from `B` to `C` is the
sum over the order-31 subgroup generated by:

```text
a^179 mod n.
```

For p24 this subgroup has order `31`, and its `179` cosets partition the
chosen degree-`5549` tensor-factor orbit.  The certificate-facing version of
this statement is now isolated in:

```text
p24/trace_frame_decimated_period_certificate_target.md
```

## Period Form

For a K-character resolvent `R_s(X)` and the selected packet coordinate
`theta`, the trace frame coordinates are:

```text
T_i(R_s) = Tr_{B/C}(theta^i R_s(theta))
         = sum_{j=0}^{30}
             theta^(i*(a^179)^j) R_s(theta^((a^179)^j)),
```

for `i=0,1,2`.

The `179` Frobenius conjugates over `E` are:

```text
sigma^t T_i(R_s)
  = sum_{j=0}^{30}
      theta^(i*a^t*(a^179)^j)
      R_s(theta^(a^t*(a^179)^j)),

0 <= t < 179.
```

So the p24 trace-frame certificate can be viewed as a `3 x 179` family of
31-term decimated H-periods.

## Meaning

This is a more arithmetic surface than a raw Moore determinant:

```text
raw one-factor Moore:    5549 Frobenius coordinates in B
twisted trace frame:     3 x 179 decimated period coordinates in C
```

The missing theorem is now:

```text
these 537 E-linear period coordinates separate the 368 selected axis
K-character resolvents.
```

Equivalently, no nonzero axis combination has all three order-31 relative
period traces equal to zero.
