# Trace-Frame Denominator-Safe Fitting Attack

Date: 2026-06-06

This note sharpens the current p24 proof attempt after isolating the
denominator-safe leading package:

```text
Xi_A, Xi_B, Xi_AB, Xi_lead
```

instead of making `Xi_tail` primary.

## Target

Let:

```text
E = F_p(mu_66254)
Q = p^5460 mod 3107441 = 209035
B/E has degree 5549
C/E has degree 179
B/C has degree 31
```

For a beta orbit `Omega` under multiplication by `Q`, let:

```text
A_Omega = O_B[Y] / phi_Omega(Y)
```

be the p-integral orbit algebra, where:

```text
phi_Omega(Y) = product_{gamma in Omega} (Y - theta^(-gamma)).
```

The denominator-free leading map is:

```text
T_lead,Omega :
  Lambda_axis tensor A_Omega
    -> O_E^368 tensor A_Omega,
```

where `Lambda_axis` is the p-integral lattice generated by:

```text
constant + 2-axis + 157-axis + 211-axis
```

and the target is the selected:

```text
179 + 179 + 10
```

normal-coordinate head of the decimated trace frame.

The missing p24 theorem is:

```text
det(T_lead,Omega) in A_Omega^*
```

for every beta orbit `Omega`, or globally:

```text
A_all = O_E[Y] / (Y^3107441 - 1)
delta_all = det(T_lead,all)
delta_all in A_all^*.
```

With tensor-factor equivariance, this is the same certificate surface as:

```text
Norm_{K_m Q(zeta_n)^<p> / K_m}(Xi_lead) mod p != 0.
```

## Fitting Form

Define:

```text
M_Omega = coker(T_lead,Omega).
```

Then:

```text
Fitt_0(M_Omega) = (det(T_lead,Omega)).
```

So the p-unit theorem is exactly:

```text
M_Omega tensor k_p = 0
```

at the selected prime over `p`.

The selected-leading failure module is:

```text
K_sel,Omega =
  { x in W_axis(A_Omega) cap F_28(A_Omega) :
      pi_10(b_28(x)) = 0 },
```

where:

```text
F_j = {x in B : deg_C(g'(theta)*x) <= j},
g'(theta)*x = b_0 + ... + b_30 theta^30,
pi_10 is the first-ten-coordinate projection on C/E.
```

Thus a failure is a nonzero CRT-axis weight family `w` with:

```text
g'(theta) * x_w in C[theta]_{<=28},
and pi_10(b_28(x_w)) = 0.
```

The full annihilator condition `W_axis cap F_27={0}` is a necessary
consequence of selected-leading injectivity, not an equivalent replacement.
The correction and the finite implication are recorded in:

```text
p24/trace_frame_selected_lead_failure_module.md
p24/lean/TraceFrameSelectedLeadFailureGate.lean
```

This is the exact selected Schubert-tail congruence to rule out.

## Denominator-Safe Prefix Use

The local factorization:

```text
Delta_lead = Delta_prefix * Delta_tail
Delta_prefix = Delta_A * Delta_B * Delta_AB
```

is still useful, but only after the determinant-line sections are fixed
globally.  The globally safe proof package is:

```text
1. prove Xi_A, Xi_B, Xi_AB are p-units;
2. prove Xi_lead is a p-unit;
3. infer the residual-tail p-unit on the open prefix chart.
```

The finite implication is Lean-checked in:

```text
p24/lean/TraceFrameDenominatorSafeLeadGate.lean
p24/lean/TraceFramePrefixTailCrossedPackageGate.lean
```

The first gate is the denominator-free `Xi_lead` route.  The second is the
compatible Fitting route: prefix p-units certify the open chart, and selected
tail crossed-product p-units rule out `K_sel` on that chart.  Together they
avoid treating an arbitrary packetwise basis of `ker Top_2` as a descended
object.

## Proof Mechanism Candidate

The most concrete route now is a Fitting-annihilator theorem.

Prove that every nonzero element of `K_Omega` would induce a nonzero element
of the CRT-axis incidence matroid kernel satisfying an additional
CM-twisted Toeplitz recurrence, and then show no such recurrence exists at
the selected prime.

In symbols, a failure gives:

```text
sum_{s in S_axis} w_s R_s(Y)
  in g'(theta)^(-1) C[theta]_{<=27} tensor A_Omega.
```

After the inverse Fourier/Toeplitz dictionary, this is a selected minor
failure of:

```text
F_T * diag(Lambda_CM) * F^{-1}_{S_axis}.
```

The CRT-axis Cauchy-Binet support is the basis set of the complete
tripartite incidence matroid with parts:

```text
2, 157, 211.
```

Therefore the proof target can be restated as:

```text
The CM-weighted basis polynomial of this incidence matroid, evaluated at the
p24 singular-moduli weights and the selected trace-frame flag, is a p-unit.
```

This is stronger than support nonvanishing.  Existing toys show:

```text
nonzero coefficients + nonzero CM weights do not prevent cancellation;
ordinary edge-weighted matrix-tree factorization is false;
component p-units do not rule out cross-axis kernels.
```

The new theorem must therefore use the actual CM weights or a determinant-line
local-unit argument, not just the matroid support.

## Literature Alignment

A targeted literature check points to a narrow external mechanism, not an
off-the-shelf proof.  Known CM-value factorization machinery applies once a
function or determinant-line section is identified as a modular/Borcherds-type
object with controlled Heegner/special-cycle divisor:

```text
Gross-Zagier/Borcherds-style CM value factorization:
  https://arxiv.org/abs/1711.02983

twisted Borcherds products and CM class-field values:
  https://arxiv.org/abs/math/0505177

local Borcherds products / local Heegner divisor obstruction language:
  https://www.numdam.org/item/AIF_2001__51_1_1_0/

p-adic theta/CM value analogues:
  https://arxiv.org/abs/2309.17251
```

This clarifies the exact missing construction:

```text
construct Xi_lead as a p-integral modular/Borcherds/Fitting object with known
divisor, then prove the selected p24 CM prime has zero local intersection with
that divisor.
```

Equivalently, identify `delta_all` with a known local-unit object in
`O_E[Y]/(Y^3107441 - 1)`.  Without this identification, CM-value
factorization theorems do not apply directly to the current determinant line.

## Matroid/Stable-Polynomial Boundary

A sidecar synthesis identified the exact combinatorial object as a mixed
Plucker, representable-matroid basis polynomial:

```text
P(lambda) = det(A diag(lambda) B)
          = sum_U Delta_A(U) Delta_B(U) prod_{u in U} lambda_u.
```

For the CRT axis, the nonzero terms are the bases of the complete tripartite
incidence matroid.  This is the correct structural name, but it does not by
itself give a new small certificate:

```text
ordinary matrix-tree / edge-product weights: ruled out;
ordinary Pfaffian compression: no visible structure;
strongly-Rayleigh/Hermitian determinantal positivity: adjacent but
  insufficient for this non-Hermitian cyclotomic CM evaluation.
```

The strong-Rayleigh warning is now exact at the support level.  The toy:

```text
p24/axis_crt_strong_rayleigh_obstruction_toy.py
```

finds a positive integer weighted Rayleigh violation for the `(2,2,3)`
tripartite incidence analogue:

```text
strong_rayleigh_violated=1
rayleigh_delta
  = -7988395388171953562263529025459059314667521815134984779980.
```

Thus real-stable/negative-dependence probability theory cannot certify the
CRT-axis Plucker noncancellation merely from the incidence support.  Any
probabilistic import must become a selected-prime p-unit theorem for the
actual mixed Plucker weights.

I reran the seconds-scale matrix-tree falsifiers:

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=p24 python3 \
  p24/axis_crt_matrix_tree_factorization_toy.py \
  --m 6 --m 10 --m 15 --max-subsets 10000 --max-pairs 500000

PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=p24 python3 \
  p24/axis_crt_matrix_tree_factorization_toy.py \
  --m 30 --max-subsets 20000 --max-pairs 500000
```

Both returned:

```text
coeff=full_coeff pair_sum_status=violated
conclusion=reported_axis_crt_matrix_tree_factorization_toy
```

The exact `m=6,10,15` analogues and sampled tripartite `m=30` analogue
therefore kill the ordinary edge-factorized matrix-tree strengthening:

```text
c(U) = C * product_{u in U} a_u.
```

The live theorem is narrower and more arithmetic:

```text
the mixed Plucker pairing
  <[A_T], wedge^368 diag(Lambda_CM,Omega) [B_axis]>
is a p-unit for every p24 packet/orbit algebra.
```

Equivalently, find a p-integral diagonal/unit equivalence that turns this
mixed Plucker pairing into a known unit object, or prove p-unitness directly
through the Fitting/local-unit theorem.

## Seconds-Scale Falsifiers

These are now the useful falsifiers for any proposed strengthening:

```text
1. Denominator-safe descent:
   python3 p24/trace_frame_schubert_descent_toy.py

   Required signature:
     fixed_sections_descend_packetwise_pivots_do_not.

2. Prefix direct position:
   python3 p24/trace_frame_prefix_intersection_audit.py
     --only-D -10919 ... --only-m 12 --max-cases 1

   Required p24-shaped signature:
     component_full=1
     intersection_minimal=1
     prefix_max_rank=1.

3. Leading local unit:
   python3 p24/trace_frame_leading_residual_value_audit.py
     --only-D -10919 ... --only-m 12 --max-cases 1

   Required signature:
     nonzero_determinant_rows=2
     zero_det_norms=0.

4. Forced low-degree kernels:
   python3 p24/trace_frame_low_degree_congruence_audit.py
     --only-D -10919 ... --only-m 12 --max-cases 1

   Required warning:
     forced kernels touch multiple axis blocks and fill the allowed low tail.

5. Strong-Rayleigh support shortcut:
   python3 p24/axis_crt_strong_rayleigh_obstruction_toy.py

   Required warning:
     strongly-Rayleigh / real-stability fails already for the tripartite
     incidence support.
```

A proposed theorem is suspect if it predicts:

```text
one-component annihilators,
sparse low tails,
ordinary packetwise pivot products,
ordinary edge-weighted matrix-tree factorization,
support-level strongly-Rayleigh positivity,
full-top-three `F_27` avoidance as a sufficient replacement for the selected
  `179+179+10` leading coordinate,
or hidden MSRD from support profiles alone.
```

## Current Best Statement To Prove

The proof-facing theorem is now:

```text
For p = 10^24 + 7, the p-integral leading trace-frame map

  T_lead,all :
    Lambda_axis tensor O_E[Y]/(Y^3107441 - 1)
      -> O_E^368 tensor O_E[Y]/(Y^3107441 - 1)

has zero reduced cokernel at the selected prime over p.
```

Equivalently:

```text
Fitt_0(coker T_lead,all) = O_E[Y]/(Y^3107441 - 1)
```

after localization at `p`.

This is the same as:

```text
delta_all in A_all^*.
```

The norm-compressed certificate then needs only a p-unit value and inverse in
`E`, plus the arithmetic theorem identifying that value with the relative
norm of `Xi_lead`.
