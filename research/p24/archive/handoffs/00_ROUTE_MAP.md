# p24 Route Map

Use this as the file-selection guide after reading
`00_HANDOFF_INDEX_20260607.md`, `00_FRESH_EYES_SYNTHESIS_20260607.md`,
`00_FRESH_AGENT_HANDOFF_20260607.md`,
`00_RETROSPECTIVE_SYNTHESIS_20260606.md`, `00_CURRENT_CONTEXT.md`,
`00_GLOBAL_SYNTHESIS_HANDOFF.md`, and `00_THEOREM_ATTEMPTS_LEDGER.md`.  The
intent is to load one narrow route at a time, not the whole folder.

## Current Main Route: Full RS-Tail Chart

Read when working on `det(Psi_RS)` and the `210 -> 156 + 54` fixed-orbit
theorem.

```text
p24/trace_gcd_rs_tail_semilinear_core_theorem.md
p24/trace_gcd_rs_tail_block_skew_cauchy_theorem_candidate.md
p24/trace_gcd_rs_tail_block_skew_cauchy_displacement_toy.py
p24/trace_gcd_rs_tail_full_plucker_chart_cauchy_toy.py
p24/trace_gcd_plucker_displacement_handoff_toy.py
p24/trace_gcd_rs_tail_shift_displacement_boundary_toy.py
p24/trace_gcd_actual_cm_full_plucker_chart_boundary.py
p24/trace_gcd_actual_cm_frequency_defect_boundary.py
p24/trace_gcd_rs_tail_frequency_defect_gate_theorem.md
p24/trace_gcd_rs_tail_frequency_defect_gate_toy.py
p24/trace_gcd_rs_tail_basis_free_frequency_gate_toy.py
p24/trace_gcd_rs_tail_frequency_moore_schur_factor_toy.py
p24/trace_gcd_rs_tail_frequency_resultant_gate.md
p24/trace_gcd_rs_tail_frequency_resultant_gate_toy.py
p24/trace_gcd_rs_tail_cyclic_section_descent.md
p24/trace_gcd_rs_tail_cyclic_section_descent_toy.py
p24/trace_gcd_actual_cm_basis_free_section_audit.py
p24/trace_gcd_rs_tail_defect_support_accounting.md
p24/trace_gcd_rs_tail_defect_support_accounting.py
p24/trace_gcd_rs_tail_fixed_frequency_ordinary_gate.md
p24/trace_gcd_rs_tail_fixed_frequency_ordinary_gate.py
p24/trace_gcd_fixed_frequency_annihilator_bridge.md
p24/trace_gcd_fixed_frequency_annihilator_bridge_toy.py
p24/trace_gcd_fixed_frequency_relation_section_toy.py
p24/trace_gcd_fixed_frequency_cyclic_syzygy.md
p24/trace_gcd_fixed_frequency_cyclic_syzygy_toy.py
p24/trace_gcd_fixed_frequency_cramer_bezout.md
p24/trace_gcd_fixed_frequency_cramer_bezout_toy.py
p24/trace_gcd_fixed_frequency_order5_collapse_toy.py
p24/trace_gcd_fixed_frequency_symmetry_boundary.md
p24/trace_gcd_fixed_frequency_symmetry_boundary.py
p24/trace_gcd_fixed_frequency_order7_augmentation.md
p24/trace_gcd_fixed_frequency_order7_augmentation_toy.py
p24/trace_gcd_fixed_frequency_order7_coset_dictionary_toy.py
p24/trace_gcd_fixed_frequency_order7_character_projection_toy.py
p24/trace_gcd_fixed_frequency_multiplicative_resolvent_bridge.md
p24/trace_gcd_fixed_frequency_multiplicative_resolvent_bridge.py
p24/trace_gcd_fixed_frequency_class_character_expansion.md
p24/trace_gcd_fixed_frequency_class_character_expansion_toy.py
p24/trace_gcd_fixed_frequency_actual_cm_right_combo_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_right_combo_boundary.py
p24/trace_gcd_fixed_frequency_p24_factor_cycle_cancellation_candidate.md
p24/trace_gcd_fixed_frequency_p24_factor_cycle_cancellation_candidate.py
p24/trace_gcd_fixed_frequency_p24_semilinear_factor_cycle_gate.md
p24/trace_gcd_fixed_frequency_p24_semilinear_factor_cycle_gate.py
p24/trace_gcd_fixed_frequency_p24_complete_factor_descent_gate.md
p24/trace_gcd_fixed_frequency_p24_complete_factor_descent_gate.py
p24/trace_gcd_fixed_frequency_p24_gauss_normalization_boundary.md
p24/trace_gcd_fixed_frequency_p24_gauss_normalization_boundary.py
p24/trace_gcd_fixed_frequency_p24_idempotent_covariance_circularity_boundary.md
p24/trace_gcd_fixed_frequency_p24_idempotent_covariance_circularity_boundary.py
p24/trace_gcd_fixed_frequency_p24_normalized_covariance_obstruction_gate.md
p24/trace_gcd_fixed_frequency_p24_normalized_covariance_obstruction_gate.py
p24/trace_gcd_fixed_frequency_p24_covariance_descent_theorem.md
p24/trace_gcd_fixed_frequency_p24_idempotent_covariance_theorem.md
p24/trace_gcd_fixed_frequency_p24_pre_recombination_covariance_gate.md
p24/lean/TraceGcdSemilinearEigenDescentGate.lean
p24/lean/TraceGcdPreRecombinationCovarianceGate.lean
p24/trace_gcd_fixed_frequency_p24_character_payload_contract.md
p24/trace_gcd_fixed_frequency_p24_character_payload_contract.py
p24/trace_gcd_fixed_frequency_p24_internal_trace_then_hilbert90_gate.md
p24/trace_gcd_fixed_frequency_p24_internal_trace_then_hilbert90_gate.py
p24/trace_gcd_fixed_frequency_p24_nested_internal_trace_gate.md
p24/trace_gcd_fixed_frequency_p24_nested_internal_trace_gate.py
p24/trace_gcd_fixed_frequency_p24_raw_coboundary_transfer_gate.md
p24/trace_gcd_fixed_frequency_p24_raw_coboundary_transfer_gate.py
p24/trace_gcd_fixed_frequency_p24_product_coboundary_leibniz_gate.md
p24/trace_gcd_fixed_frequency_p24_product_coboundary_leibniz_gate.py
p24/trace_gcd_fixed_frequency_p24_matching_twist_bookkeeping_gate.md
p24/trace_gcd_fixed_frequency_p24_matching_twist_bookkeeping_gate.py
p24/trace_gcd_fixed_frequency_p24_right_coboundary_obstruction_gate.md
p24/trace_gcd_fixed_frequency_p24_right_coboundary_obstruction_gate.py
p24/trace_gcd_fixed_frequency_p24_right_coboundary_internal_trace_gate.md
p24/trace_gcd_fixed_frequency_p24_right_coboundary_internal_trace_gate.py
p24/trace_gcd_fixed_frequency_p24_internal_trace_stage_target_gate.md
p24/trace_gcd_fixed_frequency_p24_internal_trace_stage_target_gate.py
p24/trace_gcd_fixed_frequency_p24_internal_trace_gaussian_functional_gate.md
p24/trace_gcd_fixed_frequency_p24_internal_trace_gaussian_functional_gate.py
p24/trace_gcd_fixed_frequency_p24_period_coset_balance_gate.md
p24/trace_gcd_fixed_frequency_p24_period_coset_balance_gate.py
p24/trace_gcd_fixed_frequency_p24_recombined_mixed_spectrum_gate.md
p24/trace_gcd_fixed_frequency_p24_recombined_mixed_spectrum_gate.py
p24/trace_gcd_fixed_frequency_p24_affine_profile_gate.md
p24/trace_gcd_fixed_frequency_p24_affine_profile_gate.py
p24/lean/TraceGcdAffineProfileGate.lean
p24/trace_gcd_fixed_frequency_p24_right_difference_gate.md
p24/trace_gcd_fixed_frequency_p24_right_difference_gate.py
p24/lean/TraceGcdRightDifferenceGate.lean
p24/trace_gcd_fixed_frequency_p24_right_difference_trace_gate.md
p24/trace_gcd_fixed_frequency_p24_right_difference_trace_gate.py
p24/lean/TraceGcdRightDifferenceTraceGate.lean
p24/trace_gcd_fixed_frequency_p24_right_difference_covariance_telescope_gate.md
p24/trace_gcd_fixed_frequency_p24_right_difference_covariance_telescope_gate.py
p24/lean/TraceGcdRightDifferenceCovarianceTelescopeGate.lean
p24/trace_gcd_fixed_frequency_p24_right_difference_trace_covariance_functorial_gate.md
p24/trace_gcd_fixed_frequency_p24_right_difference_trace_covariance_functorial_gate.py
p24/lean/TraceGcdRightDifferenceTraceCovarianceFunctorialGate.lean
p24/trace_gcd_fixed_frequency_p24_adjacent_anchor_descent_gate.md
p24/trace_gcd_fixed_frequency_p24_adjacent_anchor_descent_gate.py
p24/lean/TraceGcdAdjacentAnchorDescentGate.lean
p24/trace_gcd_fixed_frequency_p24_adjacent_difference_operator_gate.md
p24/trace_gcd_fixed_frequency_p24_adjacent_difference_operator_gate.py
p24/lean/TraceGcdAdjacentDifferenceOperatorGate.lean
p24/trace_gcd_fixed_frequency_p24_mixed_spectrum_resolvent_bridge.md
p24/trace_gcd_fixed_frequency_p24_mixed_spectrum_resolvent_bridge.py
p24/trace_gcd_fixed_frequency_actual_cm_mixed_spectrum_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_mixed_spectrum_boundary.py
p24/trace_gcd_fixed_frequency_p24_anchor_trace_defect_gate.md
p24/trace_gcd_fixed_frequency_p24_anchor_trace_defect_gate.py
p24/trace_gcd_fixed_frequency_p24_trace_average_anchor_payload_gate.md
p24/trace_gcd_fixed_frequency_p24_trace_average_anchor_payload_gate.py
p24/trace_gcd_fixed_frequency_p24_anchor_vs_c_centering_boundary.md
p24/trace_gcd_fixed_frequency_p24_anchor_vs_c_centering_boundary.py
p24/trace_gcd_fixed_frequency_p24_section_choice_obstruction_gate.md
p24/trace_gcd_fixed_frequency_p24_section_choice_obstruction_gate.py
p24/p24_subsqrt_scale_discipline_gate.md
p24/p24_subsqrt_scale_discipline_gate.py
p24/trace_gcd_fixed_frequency_p24_internal_character_filter_gate.md
p24/trace_gcd_fixed_frequency_p24_internal_character_filter_gate.py
p24/trace_gcd_fixed_frequency_actual_cm_internal_character_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_internal_character_boundary.py
p24/trace_gcd_fixed_frequency_p24_right_gauss_weighted_polynomial_gate.md
p24/trace_gcd_fixed_frequency_p24_right_gauss_weighted_polynomial_gate.py
p24/trace_gcd_fixed_frequency_p24_right_axis_spectrum_gate.md
p24/trace_gcd_fixed_frequency_p24_right_axis_spectrum_gate.py
p24/trace_gcd_fixed_frequency_p24_right_axis_covariance_descent_gate.md
p24/trace_gcd_fixed_frequency_p24_right_axis_covariance_descent_gate.py
p24/trace_gcd_fixed_frequency_p24_right_axis_fixed_field_refinement_gate.md
p24/trace_gcd_fixed_frequency_p24_right_axis_fixed_field_refinement_gate.py
p24/trace_gcd_fixed_frequency_p24_right_axis_anchor_projector_gate.md
p24/trace_gcd_fixed_frequency_p24_right_axis_anchor_projector_gate.py
p24/trace_gcd_fixed_frequency_p24_right_axis_projector_character_bridge.md
p24/trace_gcd_fixed_frequency_p24_right_axis_projector_character_bridge.py
p24/trace_gcd_fixed_frequency_p24_projector_internal_character_target_gate.md
p24/trace_gcd_fixed_frequency_p24_projector_internal_character_target_gate.py
p24/trace_gcd_fixed_frequency_p24_right_c_bidegree_support_gate.md
p24/trace_gcd_fixed_frequency_p24_right_c_bidegree_support_gate.py
p24/trace_gcd_fixed_frequency_p24_stickelberger_bidegree_boundary.md
p24/trace_gcd_fixed_frequency_p24_stickelberger_bidegree_boundary.py
p24/trace_gcd_fixed_frequency_p24_jacobi_carry_c_centering_gate.md
p24/trace_gcd_fixed_frequency_p24_jacobi_carry_c_centering_gate.py
p24/trace_gcd_fixed_frequency_p24_jacobi_carry_span_boundary.md
p24/trace_gcd_fixed_frequency_p24_jacobi_carry_span_boundary.py
p24/trace_gcd_fixed_frequency_p24_jacobi_carry_fourier_formula_gate.md
p24/trace_gcd_fixed_frequency_p24_jacobi_carry_fourier_formula_gate.py
p24/trace_gcd_fixed_frequency_p24_admissible_jacobi_spectral_boundary.md
p24/trace_gcd_fixed_frequency_p24_admissible_jacobi_spectral_boundary.py
p24/trace_gcd_fixed_frequency_p24_admissible_jacobi_dual_conditions_gate.md
p24/trace_gcd_fixed_frequency_p24_admissible_jacobi_dual_conditions_gate.py
p24/trace_gcd_fixed_frequency_p24_dual_condition_source_map.md
p24/trace_gcd_fixed_frequency_p24_dual_conditions_value_side_gate.md
p24/trace_gcd_fixed_frequency_p24_dual_conditions_value_side_gate.py
p24/trace_gcd_fixed_frequency_p24_value_identity_strength_gate.md
p24/trace_gcd_fixed_frequency_p24_value_identity_strength_gate.py
p24/trace_gcd_fixed_frequency_p24_selected_defect_value_producer_gate.md
p24/trace_gcd_fixed_frequency_p24_selected_defect_value_producer_gate.py
p24/trace_gcd_fixed_frequency_p24_multiplicative_producer_dictionary_gate.md
p24/trace_gcd_fixed_frequency_p24_multiplicative_producer_dictionary_gate.py
p24/trace_gcd_fixed_frequency_jacobi_sum_product_formula_probe.md
p24/trace_gcd_fixed_frequency_jacobi_sum_product_formula_probe.py
p24/trace_gcd_fixed_frequency_jacobi_sum_anchor_defect_theorem.md
p24/trace_gcd_fixed_frequency_jacobi_sum_row_ratio_miner.py
p24/trace_gcd_fixed_frequency_jacobi_sum_anchor_correction_gate.md
p24/trace_gcd_fixed_frequency_jacobi_sum_anchor_correction_gate.py
p24/trace_gcd_fixed_frequency_jacobi_sum_anchor_scalar_search.md
p24/trace_gcd_fixed_frequency_jacobi_sum_anchor_scalar_search.py
p24/trace_gcd_fixed_frequency_jacobi_anchor_residual_factor_search.md
p24/trace_gcd_fixed_frequency_jacobi_anchor_residual_factor_search.py
p24/trace_gcd_fixed_frequency_jacobi_anchor_kummer_descent_gate.md
p24/trace_gcd_fixed_frequency_jacobi_anchor_kummer_descent_gate.py
p24/trace_gcd_fixed_frequency_jacobi_sum_punctured_hasse_davenport_theorem.md
p24/trace_gcd_fixed_frequency_jacobi_sum_symbolic_hd_gate.md
p24/trace_gcd_fixed_frequency_jacobi_sum_symbolic_hd_gate.py
p24/trace_gcd_fixed_frequency_reduced_anchor_fingerprint_gate.md
p24/trace_gcd_fixed_frequency_reduced_anchor_fingerprint_gate.py
p24/trace_gcd_fixed_frequency_reduced_anchor_adjacent_bridge_gate.md
p24/trace_gcd_fixed_frequency_reduced_anchor_adjacent_bridge_gate.py
p24/trace_gcd_fixed_frequency_reduced_anchor_slice_decomposition_gate.md
p24/trace_gcd_fixed_frequency_reduced_anchor_slice_decomposition_gate.py
p24/trace_gcd_fixed_frequency_reduced_anchor_cyclotomic_divisor_gate.md
p24/trace_gcd_fixed_frequency_reduced_anchor_cyclotomic_divisor_gate.py
p24/trace_gcd_fixed_frequency_p24_admissible_jacobi_decomposition_theorem.md
p24/lean/TraceGcdJacobiAnchorCorrectionGate.lean
p24/lean/TraceGcdReducedAnchorAdjacentBridgeGate.lean
p24/lean/TraceGcdReducedAnchorSliceDecompositionGate.lean
p24/lean/TraceGcdReducedAnchorCyclotomicDivisorGate.lean
p24/lean/TraceGcdAnchorKummerDescentGate.lean
p24/lean/TraceGcdAdmissibleJacobiDecompositionGate.lean
p24/lean/TraceGcdAdmissibleJacobiDualConditionsGate.lean
p24/lean/TraceGcdDualConditionsValueSideGate.lean
p24/trace_gcd_fixed_frequency_actual_cm_admissible_jacobi_span_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_admissible_jacobi_span_boundary.py
p24/trace_gcd_fixed_frequency_actual_cm_value_identity_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_value_identity_boundary.py
p24/lean/TraceGcdRightCBidegreeSupportGate.lean
p24/trace_gcd_fixed_frequency_actual_cm_projector_internal_character_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_projector_internal_character_boundary.py
p24/trace_gcd_fixed_frequency_actual_cm_adjacent_anchor_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_adjacent_anchor_boundary.py
p24/lean/TraceGcdRightAxisAnchorDescentGate.lean
p24/trace_gcd_fixed_frequency_actual_cm_right_combo_internal_trace_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_right_combo_internal_trace_boundary.py
p24/trace_gcd_fixed_frequency_actual_cm_product_internal_trace_boundary.md
p24/trace_gcd_fixed_frequency_actual_cm_product_internal_trace_boundary.py
p24/trace_gcd_fixed_frequency_p24_twisted_hilbert90_payload_gate.md
p24/trace_gcd_fixed_frequency_p24_twisted_hilbert90_payload_gate.py
p24/lean/TraceGcdProductCoboundaryGate.lean
p24/lean/TraceGcdProjectorTracePipelineGate.lean
p24/lean/TraceGcdRecombinedMixedSpectrumGate.lean
p24/trace_gcd_fixed_frequency_p24_projector_trace_pipeline_gate.md
p24/trace_gcd_fixed_frequency_order7_h_coboundary.md
p24/trace_gcd_fixed_frequency_order7_h_coboundary_toy.py
p24/trace_gcd_fixed_frequency_order7_h_bezout_operator_toy.py
p24/trace_gcd_fixed_frequency_h_coboundary_basefield_boundary.md
p24/trace_gcd_fixed_frequency_h_coboundary_basefield_boundary.py
p24/trace_gcd_fixed_frequency_p24_h_coset_sum_verifier.py
p24/trace_gcd_fixed_frequency_h_kernel_inclusion_gate.md
p24/trace_gcd_fixed_frequency_h_kernel_inclusion_gate.py
p24/trace_gcd_fixed_frequency_h_coset_selector_boundary.md
p24/trace_gcd_fixed_frequency_h_coset_selector_boundary.py
p24/trace_gcd_fixed_frequency_order7_paired_potential_boundary_toy.py
p24/trace_gcd_fixed_frequency_order7_rank_compatibility_toy.py
p24/trace_gcd_fixed_frequency_unit_symmetry_boundary.md
p24/trace_gcd_fixed_frequency_unit_symmetry_boundary.py
p24/trace_gcd_fixed_frequency_relation_shape_index.py
```

Question to answer:

```text
Do the selected/omitted blocks M_SS, M_OO of the common cyclic/Lang shift give
the exact graph/Riccati identity, hence small-rank pure displacement
M_SS C - C M_OO from only the split tail boundary?

Before forming C, can the frequency-defect gate prove the selected 156
coordinates are a p-unit basis from 19 ordinary local Plucker p-units, 16
defect tail residues, and the 16x16 tail Vandermonde p-unit?

Can this be proved basis-free from local projection-rank p-units and
prefix-to-prefix-plus-tail rank jumps, so the actual CM/Lang spectral rows do
not need a friendly local basis?

Do these local factors match the existing Moore/Schur split exactly, so the
prefix Moore factor is the product of local Plucker determinants and the
quotient-tail Moore factor is the defect-residue Vandermonde?

Can those local factors be realized as cyclic polynomial sections
`P_24,T_24,S_24`, turning the local checks into resultant/Bezout p-unit
certificates plus selector support?

Do the proposed local values satisfy semilinear Frobenius descent
`F_{p a}=F_a^p`, and which of the `1260` Frobenius-stable size-16 defect
supports occurs?  Can we prove no fixed-frequency defects, reducing the
selector choices to the `35` pure length-4 supports?

At the seven fixed frequencies `a in 5Z/35Z`, can we prove the local tail
value lies in the prefix image, i.e. `rank(P_a,tau_a)=rank(P_a)`?  This is the
current no-fixed-defect arithmetic lemma.

Can this be proved dually as a trace-annihilator inclusion
`Ann(V_{a,2},V_{a,3},V_{a,5},V_{a,6}) subset Ann(V_{a,1})`, giving seven
fixed-frequency linear relations without enumerating the class set?

Can those seven relations be constructed as one cyclic `R_7 = F_p[y]/(y^7-1)`
syzygy, rather than as post-fit pointwise coefficients?  Since `p = 1 mod 7`,
pointwise relations interpolate automatically; the arithmetic work is the
intrinsic construction of the cyclic coefficient sections.

Can that cyclic syzygy be upgraded to a Cramer/Bezout certificate
`D T = sum N_j P_j` with `D in R_7^*`, so the denominator unit is exactly a
fixed-prefix Plucker gate and the full vector identity supplies the missing
tail-in-prefix content?

Can the fixed-frequency sections be constructed using only the order-5
collapsed sums along the length-35 right orbit?  The collapse toy proves the
seven fixed frequencies need only the 7-part, while nonfixed frequencies are
not determined by that collapse.

Do not try to prove the nontrivial fixed-frequency relations from centering
plus right sign/Hermitian symmetry alone.  The symmetry boundary shows that
these force the orbit-1 relation only at `a=0`, not at the six nontrivial
order-7 fixed frequencies.

Can the missing extra input be the order-7 augmentation identity
`T+P_2+P_3+P_4+P_5+P_6=0` over `R_7`, together with the computed negation
covariance `P_4=y^{-2}T`?  If yes, then `1+y^{-2}` is a unit and gives an
explicit no-fixed-defect syzygy.

Equivalently, can we prove the seven order-30 subgroup coset sums vanish in
the right frequency group?  The coset dictionary identifies ordinary centering
as only the trivial character of this order-7 quotient; the missing content is
the six nontrivial quotient-character vanishings.  The character-projection
toy sharpens this again: for
`S_v=sum_s zeta_211^(v*s)G_s`, the nontrivial augmentations are exactly
nonzero Gauss sums times
`sum_{s != 0} chi_k(s)^(-1)G_s`, so the arithmetic theorem is a right
order-7 multiplicative-profile vanishing, not a generic centering identity.
Equivalently, this is a mixed-resolvent orthogonality theorem:
`<A_1, sum_v chi_k(v)B_v>=0` for the six right multiplicative order-7
resolvents.  Do not try to prove it from Frobenius covariance plus centering:
`p^156` contributes the expected nontrivial eigenvalue to the Gauss-sum factor,
so the divided `L`-valued projection can remain nonzero.
In relative class-character language the same scalar is a packet product sum
`sum_a T_{1,0,a} * R_{chi,-a}`.  Therefore nonzero relative resolvents are not
enough; the proof needs packet cancellation, or the stronger termwise theorem
`R_{chi,-a}=0` for every relative packet character.
The p24 factor-cycle alignment must be used semilinearly: `p^780` has order
7 on `E` and fixes `L=F_p(mu_157)`, so covariance only places the packet sum
in a nontrivial Frobenius eigenspace.  The zero follows after proving the
same total sum descends to the `p^780`-fixed left field.  Do not replace this
with scalar geometric cancellation unless every cycle seed is already fixed.
The descent input is complete recombination over all 70 E-tensor idempotents:
`S_chi=sum_delta Z_delta(chi)` must be the original `L`-valued projection.
One representative factor or one 7-cycle is not a descended certificate.
The remaining arithmetic theorem is the idempotent covariance identity
`rho(Z_delta(chi))=lambda_chi^(-1)Z_{delta+10}(chi)` for this complete
decomposition.  Here `lambda_chi=chi(p^780 mod 211)` is nontrivial for every
nontrivial order-7 quotient character.
There is an internal-trace caveat.  On a raw relative factor cycle, `p^780`
has order `38843 = 7*5549`, and after seven steps `p^5460` acts internally
with order `5549 = 31*179`.  Thus the quotient length-7 Hilbert-90 theorem
applies only after the internal degree-5549 trace/norm has produced an
E-valued seed.  This internal stage factors as
`Tr_{B/E}=Tr_{C/E} o Tr_{B/C}` with degrees `179` and `31`; either partial
trace alone is not quotient-fixed enough.  Equivalently, after choosing one
traced seed on each of the
ten length-7 quotient cycles, the seed must be a twisted Hilbert-90
coboundary `sigma(Y)-epsilon*Y`; finite linear algebra gives
`im(sigma-epsilon)=ker(Tr_epsilon)`.  This is now the most explicit embedded
tower target: construct either the CM/Lang internal trace and quotient
potential, or more strongly a raw full-order potential before tracing.  The
nested internal trace commutes with raw coboundaries, so a raw potential would
descend automatically; constructing it by inverting Hilbert-90 after trace
zero would be circular.
The current sharpened sufficient route is a product-coboundary identity for
the packet terms `T_{1,0,a} * R_{chi,-a}`: prove
`sigma(T_{1,0,a})=alpha_a*T_{1,0,a}` and construct
`R_{chi,-a}=sigma(V_{chi,a})-(epsilon_chi/alpha_a)*V_{chi,a}`.  The
product-Leibniz gate and Lean formalization then give the raw coboundary
before any trace-zero inversion.  Wrong twists and random right factors fail
in the finite model, so the matching twist is real arithmetic content.  The
p24 twist bookkeeping is now explicit: in the raw convention
`chi_k(2)=zeta_7^k`, `alpha_a=1` and `epsilon_chi=zeta_7^k`; the normalized
convention used by some earlier toys has the same theorem with quotient
characters relabeled.  However, formal right-character covariance has this
same eigenvalue and is therefore the obstruction eigenspace of
`sigma-epsilon_chi`, not a matching potential.  The right-coboundary route
requires additional CM/Lang internal-trace cancellation; pure cyclotomic
degree-5549 Gaussian periods do not vanish automatically.  The positive
replacement is exact: matching right coboundary is equivalent to nested
internal-trace zero.  Prove this internal trace identity from CM/Lang packet
structure first; only then use the formal Hilbert-90 inverse.  Do not
strengthen this to `Tr_{B/C}=0` without new arithmetic: the stage-target gate
shows that is a codimension-179 p24 analogue, while the actual nested target
is codimension one, namely `Tr_{C/E}(Tr_{B/C}(R_obstruction))=0`.  Expanded
on relative polynomial coefficients this is
`sum_k c_k eta_{ak}=0`, where
`eta_t=sum_{r in <p^5460>} zeta_n^(tr)`.  This weighted Gaussian-period
cancellation is not implied by augmentation/content nonvanishing.  Inverting
the quotient period matrix sharpens the strong per-factor target to
coefficient balance:
for every nonzero `<p^5460>`-coset `D` in `F_n^*`,
`sum_{k in D} c_k(chi) = 5549*c_0(chi)`.  There are `560` such cosets for
p24, so this route needs a real weighted CM-sequence equidistribution theorem
unless it instead proves cross-factor recombination cancellation directly.
After complete recombination over the `70` E-idempotents in one Fp packet, the
same inversion uses `W=<p>` of order `388430`; the balance target drops to
the `8` nonzero `<p>`-cosets:
`sum_{k in D} c_k(chi)=388430*c_0(chi)`.  Prefer this 8-coset formulation for
the next arithmetic attack; keep the 560-coset formulation only as the
stronger per-factor warning.  For the six nontrivial right quotient
characters this gives a `48`-scalar compressed verifier, split as `42`
nontrivial octic quotient equations plus `6` anchor equations, after the tower
proof has produced the eight coset sums and `c_0` anchors.
Equivalently, with `K_n=Q(zeta_n)` and `M=K_n^<p>` of degree `8`, prove
`Tr_{K_n/M}(G_chi(zeta_n)) == 0` modulo the selected prime for the six
Gauss-normalized right quotient characters.
The current right-difference trace route sharpens this to the seven adjacent
traces
`T_i=Tr_{K_n/M}(P_i(zeta_n))`.  Pointwise CM/Lang Frobenius functoriality of
`P_i` gives `T_{i+6}=rho(T_i)` because `rho=p^780` lies in `<p>`, and
telescoping gives `sum_i T_i=0`.  The remaining descent input is the single
anchor `rho(T_0)=T_0`, equivalently six nontrivial order-7 rho-projector
vanishings `Pi_k(T_0)=0`.  This is now the smallest proof target for the
compressed `48` equations; the raw `1092` H-coset equations are a verifier
surface, not the theorem to enumerate.
The adjacent-difference operator gate then identifies `T_0` with
`(rho^6-1)Y_0` for the right-axis H-coset profile `Y_i`, because the p24
right shift is `6=-1 mod 7`.  The operator `rho^6-1` has rank `6`, kernel the
rho-fixed line, and nonzero projector multipliers `omega^(6k)-1` for
`k=1,...,6`; hence adjacent-anchor descent is equivalent to the old
right-axis equal-H-coset descent theorem.  The remaining arithmetic theorem is
therefore exactly: the selected trace-GCD weighted/section-aware internally
traced right profile has no nontrivial order-7 component.
The actual-CM adjacent-anchor boundary checks this exact formal shape on the
small right-axis row `D=-6719`, `q=6863`, `h=105`, `m=21=3*7`, `n=5`.
There covariance and telescope hold for adjacent H-coset trace differences,
but the anchor descends in `0/2` relative trace cosets.  Therefore the p24
proof needs the special trace-GCD weighted/selected adjacent packet; ordinary
actual CM covariance is not enough.
The six anchor equations have a sharper arithmetic form.  They are equivalent
to the vanishing of the six nontrivial right quotient-character projections of
the relative trace defect
`D_r=Tr_relative(j_{r+m*bullet})-n*j_r`; equivalently, this defect profile has
equal `H=<2^7>` coset sums on `F_211^*`.  This is now the preferred anchor
target.  As a verifier surface, seven honest defect H-coset sums are enough;
the fuller quotient trace-average plus selected-child profile has size
`2*m=132508`, still only `1.32508e-7*sqrt(p)`.  Producer honesty remains the
hard part: fake equal sums pass unless tied to an embedded CM/Lang
construction.
The anchor-vs-C-centering boundary prevents a nearby false compression:
trace-defect anchor zero does not imply the forbidden `C/E`-trivial bidegree
vanishes, and `C/E`-centering does not imply the section-aware anchor.  In the
exact quotient model `C_7 x C_179 x C_31`, selected-section subtraction can
mask a forbidden bidegree in `32/32` controls.  A proof must control the
selected child right profile, prove direct `C/E`-centering, or prove the full
affine profile identity tying both together.
This must be Gauss-normalized.  Formal Frobenius covariance of the
unnormalized additive resolvent only gives the Gauss-sum eigenvalue, and the
divided `L`-valued projection can remain nonzero.
Also do not infer nontrivial idempotent covariance after descent: descended
idempotent components have eigenvalue `1`, so nontrivial covariance is
equivalent to vanishing.  The noncircular proof must establish
Gauss-normalized covariance on the trace-resolvent summands before complete
recombination.
Equivalently, with `H=<2^7>` of order `30`, prove the right profile is an
additive Hilbert-90 coboundary `G_s=Y_s-Y_{2^7s}`; this is the explicit
embedded-tower potential form of the same H-coset trace vanishing.
The potential is canonical once the trace identity is proved:
`Y=UG`, where `U=(1/30)sum_{i=0}^{29}(29-i)T^i` and
`(1-T)U=1-e_H`.  So the remaining arithmetic theorem is `e_HG=0`, not a
search for `Y`.
In base-field terms this is the row-wise vanishing of the seven
multiplicative Gaussian-period column sums of the centered `156 x 210`
mixed marginal matrix `C(r,s)`.
The newest internal-character refinement says the same codimension-one
condition is zero trivial `C/E` character component after the `B/C` trace.
The order-7 quotient twist is not enough: after seven raw `p^780` steps the
order-7 scalar has disappeared and the internal generator has begun.  The
remaining theorem must therefore place the `B/C`-traced obstruction in
nontrivial `C/E` character support, or otherwise kill its trivial component.
The actual-CM boundary prevents overgeneralizing this: raw embedded CM period
packets in the `D=-5000`, `h=30=2*5*3` calibration have nonzero trivial
`C` projection in every rotated/top packet.  The p24 proof has to exploit the
right-obstruction/product-coboundary structure, not just CM-periodness.
The right-obstruction structure is now explicit after Gauss-sum reduction:
`R_{chi,-a}=tau(chi)*G_chi(zeta_n^{-a})`, where
`G_chi(X)=sum_r chi^{-1}(r mod 211)F_r(X)` and
`F_r(X)=sum_k j_{r+m*k}X^k`.  So the next theorem should be stated directly
as zero trivial `C/E` projection for the `B/C` trace of this `G_chi`, rather
than for raw periods or arbitrary weighted polynomials.
Since `p^5460 = 1 mod 211`, the internal trace fixes the right axis instead
of averaging it.  Thus the same theorem is no order-7 multiplicative
spectrum in the internally traced `G_chi` right profile, equivalently equal
H-coset sums on `F_211^*`.  The right-axis covariance/descent gate makes the
sufficient finite theorem precise: if those seven sums satisfy
`Y_{c+6}=rho(Y_c)` under `rho=p^780` and one anchor sum descends to the
`rho`-fixed field, then all seven sums are equal.  Under covariance this
anchor descent is equivalent to zero order-7 spectrum.  The fixed-field
refinement gate shows `E^rho` has degree `780=156*5`; descent to
`F_p(mu_157)` is sufficient but stronger than needed.  Pure right H-periods
are covariant but not anchor-fixed, so the actual `G_chi` coefficients must
cancel the nonfixed right-period part.  The anchor-projector gate rewrites
this as six explicit equations `Pi_k(Y_0)=0`, `k=1,...,6`, for the
nontrivial rho-eigenprojectors.  The projector-character bridge identifies
these with the six nontrivial H-quotient character equations by the p24
index map `m -> 6m mod 7`.  The projector/internal-character target gate
then combines this with the nested trace target: prove that every nontrivial
projector channel has zero trivial `C/E` component after `Tr_{B/C}`.
The right/C bidegree support gate names the exact forbidden Fourier slots:
after the `B/C` trace, the six bidegrees
`(right character k, C/E character 0)`, `k=1,...,6`, must vanish.  Final
internal trace zero is equivalent to the absence of these slots.  Since
`gcd(7,179)=1`, this cannot be a nontrivial quotient homomorphism from the
right quotient to the `C/E` character group; any support separation must come
from the selected trace-GCD weighted/section-aware CM packet.
The Stickelberger bidegree boundary checks the first Jacobi-sum shortcut
against the same support condition: plain cyclic Stickelberger on
`C_(7*179)` and plain right-axis Stickelberger both leak in all six forbidden
slots, even after centering.  Only a deliberately `C/E`-centered product has
the right finite support, so a successful Stickelberger proof must explain
that centering from the selected weighted packet itself.
The Jacobi-carry C-centering gate gives a narrow positive shape: a Jacobi
carry `theta_{u,v}=[ut]+[vt]-[(u+v)t]` with one right-trivial nontrivial
`C/E` input has zero forbidden bidegrees (`48/48`), while generic Jacobi
carries, pure-right partners, and C-cancelled sums leak (`48/48`).  Thus a
Jacobi-sum proof is plausible only as a decomposition of the weighted packet
into C-axis Jacobi carries.
The admissible Jacobi-carry span boundary corrects the rank target for this
positive shape.  The termwise-safe family requires the partner and the sum to
keep nontrivial `C/E` component; its p24 rank is `621`.  The broader C-axis
family has rank `625`, but includes four leaky directions from pure-right
partners and `C/E`-cancelling carries.  Therefore the clean theorem is:
the weighted packet after `Tr_{B/C}` lies in the rank-`621` admissible span.
A broader rank-`625` theorem is valid only with an extra proof that the four
leaky directions cancel.
The Jacobi-carry Fourier formula gate gives a symbolic proof for the four
dual condition families on each admissible carry.  The sawtooth formula gives
conjugate skew by cancellation of `hat g_m(k)+hat g_m(-k)=-d*N`, the
right-trivial pair-sum scalar is `lambda_c=-2/(c-1)` (so p24 has
`lambda_179=-1/89`), and the three global balances come from vanishing on the
`C`-zero fiber.  This sharpens the arithmetic target to constructing the
selected packet from admissible carries, or proving these same symbolic
identities directly for it.
The admissible Jacobi spectral boundary gives the rank-`621` target a Fourier
fingerprint.  In small exact models, the `C/E`-trivial slice has rank `1`,
each nontrivial `C/E` slice has full right rank `7`, each conjugate
`C/E`-pair has rank `8` rather than `14`, and cumulative pair increments are
`1,7,...,7,4`.  For p24 this is `1 + 7*88 + 4 = 621`.  Thus the proof should
target conjugate-`C/E` pair compatibility, not merely zero forbidden support.
The admissible Jacobi dual-conditions gate turns that fingerprint into four
explicit Fourier condition families: forbidden right-nontrivial `C/E`-trivial
coefficients vanish; nontrivial right coefficients are skew under
`(a,b)->(-a,-b)`; right-trivial conjugate C/E pair sums share one normalized
constant term; and three global pair-balance equations hold.  For p24 these
are `632` independent equations cutting the `1253`-dimensional Fourier space
down to the rank-`621` admissible span.
The Lean dual-conditions gate records that these four families, once proved
for the selected weighted packet, feed the existing
admissible-span/no-forbidden-bidegree/final-trace/product-coboundary/H-coset
pipeline without any additional finite theorem.
The dual-condition source map records the proof obligations family by family:
forbidden `C/E`-trivial vanishing needs direct centering rather than anchor
zero; conjugate skew needs real anti-invariance rather than generic inversion
symmetry; right-trivial pair sums likely come from a normalization/product
formula; and the three global balances are the natural meeting point with the
right-difference telescope route.
The dual-conditions value-side gate translates the four Fourier families into
three packet-facing identities: C-row sums are independent of the right
coordinate, the packet vanishes on the C-zero fiber, and
`f(r,c)+f(-r,-c)` is one constant for all `c != 0`.  This is probably the
best form in which to attack the selected CM/Lang packet directly.
The value-identity strength gate separates this into a structural symmetry
part and a small balance part: `f(r,0)=0` plus constant inversion-complement
has rank `629` for p24, leaving only three independent row-sum/global-balance
equations to reach the rank-`621` admissible target.
The selected-defect value producer gate moves one layer closer to the raw
packet: for `f(r,c)=g(r,c)-g(r,0)`, the target is equivalent to a two-level
raw inversion complement
`g(r,0)+g(-r,0)=A_0`, `g(r,c)+g(-r,-c)=A_1` for `c != 0`, plus the selected
affine row balance `sum_c g(r,c)-179*g(r,0)` independent of `r`.
The multiplicative producer dictionary translates this into the natural
product-formula target: construct a torus/modular-unit packet `U(r,c)` with
constant pair-products `U(r,0)U(-r,0)`, `U(r,c)U(-r,-c)` and constant selected
row-product ratio `prod_c U(r,c)/U(r,0)^179`.
The Lean value-side gate records the same handoff as a formal contract from
those three identities to the existing verifier pipeline.
The actual-CM admissible Jacobi-span boundary tests nearby real rows rather
than synthetic quotient distributions.  The raw `D=-5000` projector row has
`0/30` admissible-span origins.  The pinned `D=-13319` row has `0/140` for
right-combo resolvents, raw weighted coefficients, and selected-child defect
coefficients; all also have `0` broad-span origins.  Thus admissible
membership is not generic embedded-CM/projector/right-combo/coefficient
structure.  The next useful positive computation must materialize the
selected weighted trace-GCD packet or a faithful small analogue, then test
rank-`621` membership and mine conjugate-pair formulas.
The actual-CM value-identity boundary refines the failure by the new
packet-facing identities.  Generic projector/right-combo/weighted coefficient
rows fail all three identities.  Selected-defect coefficients force only
`f(r,0)=0`; they still fail inversion-complement and row-sum balance.
The Lean projector-trace pipeline gate proves that this one arithmetic input,
plus the already-separated centering/coboundary handoffs, implies the `1092`
H-coset verifier.  So do not add more finite handoff layers here; prove the
six projected weighted-packet final trace identities.
Covariance alone gives conjugate sums
and descent alone gives arbitrary fixed sums, so neither input can be dropped.
The actual-CM projector/internal-character boundary rules out the shortcut
"nontrivial projector implies zero final internal trace": its `D=-5000`,
`h=30=2*5*3` calibration has `30/30` nontrivial projected packets with
nonzero final trace.
The pinned actual-CM right-combo boundary also rules out the generic shortcut
"right-combo obstruction implies internal trace zero": its two internal
right-combo traces are both nonzero.  The right-combo anchor boundary isolates
the recombined anchor equation on the same row: because `n=5` and `<q>` is
all of `F_5^*`, there are no nontrivial quotient equations, and the actual
right-combo `G_chi` analogue fails `sum_{k=1}^4 c_k=4*c_0`.  Thus the p24
anchor is not generic decomposition-field trace language.  Rotating all `140`
global origin/section choices in that row still gives `0/140` anchor passes,
so section choice alone is not the missing generic theorem.  The
product/internal-trace boundary
rules out the stronger-looking shortcut "weighted product packet shape implies
internal trace zero": its two weighted product traces are also both nonzero,
and its recombined `<q>`/decomposition-field product trace is nonzero.
The p24 proof must explain equal H-coset sums for the specific internally
traced `G_chi` profile.
Equivalently, because `p=1 mod 7`, ordinary centering plus the six nontrivial
order-7 quotient-character sums has full rank on the seven H-coset sums.
Thus the live tower payload is six `L`-valued zeros, or `6*156 = 936`
nontrivial scalar coordinates, plus the `156` centering coordinates.
The H-coset selectors have full additive Fourier support: each nontrivial
quotient-character selector has nonzero coefficients at all `210` nonzero
right frequencies, so this is not a sparse right-frequency shortcut.
Do not automatically strengthen this to a full right-resolvent potential
`B_s=Z_s-Z_{2^7s}`: that is sufficient, but a finite boundary shows the paired
`L`-profile can have a potential while the unpaired right-resolvent family has
H-trace leakage in the pairing kernel.
The rank-compatibility toy shows this target is neither impossible nor already
explained by generic linear algebra: seven coset constraints leave dimension
`203 > 156`, while ordinary centering plus full row rank still has a six-rank
nontrivial quotient leak.

A limited relation-shape prefilter found no cheap actual-CM calibration row
with the p24 fixed-frequency geometry: in the default limited scan, 3000
q-tests give `hits=0` and `right_component_cases=0`.  So do not wait on small
CM rows for this theorem unless the search bounds or shape constraints change.

Do not replace the H-coboundary theorem with naive right-unit multiplier
invariance.  Multiplier invariance would be sufficient, but the pinned
actual-CM analogue `D=-13319, q=13463, left=4, right=7` has nonzero quotient
projection and `18` multiplier-invariance failures.  Right-unit/diamond
transport is p-unit transport between determinant lines, not literal
invariance of the embedded mixed Hermitian profile.

Can a smaller actual-CM calibration row test this exact fixed-frequency shape?
The relation-shape index is the cheap prefilter: it requires `right_len=35`,
`q mod 35 = 22`, and a non-tail-only `left_len > 35` prefix/tail split before
any Hilbert-polynomial work.

Do the available smaller actual-CM rows calibrate the frequency-defect local
profile?  Currently no: the audit finds no clean p24-like shape and no row
passing the local frequency-profile gate.

For those rows, is the obstruction descent or support size?  The basis-free
section audit says rank-profile Frobenius covariance holds on `10/10` rows,
but the nontrivial prefix+tail rows have wrong defect-support size (`4/4`),
so the analogous p24 proof needs an intrinsic selector theorem.
```

Do not spend time re-testing visible scalar GRS unless changing the theorem.

## Fixed-Square Linear Algebra

Read when touching the explicit `Psi_RS` construction, Hilbert-90 descent, or
the Moore/Schur split.

```text
p24/trace_gcd_full_gaussian_rs_tail_target.md
p24/trace_gcd_full_gaussian_rs_tail_toy.py
p24/trace_gcd_actual_cm_gaussian_rs_tail_audit.py
p24/trace_gcd_actual_cm_rs_tail_semilinear_core_audit.py
p24/trace_gcd_rs_tail_fixed_adjoint_toy.py
p24/trace_gcd_rs_tail_syndrome_moore_schur_toy.py
p24/trace_gcd_rs_tail_block_support_profile_toy.py
```

Question to answer:

```text
Can the explicit `F_p^28 + K^28 + F_p^16 -> L` map be proved p-unit by a
named determinant identity instead of a rank scan?
```

## Nonzero-Orbit Crossed Norm

Read when working on `Xi_O1 = Nrd_O(Phi_t)` and unit transport.

```text
p24/trace_gcd_two_resultant_proof_route_synthesis.md
p24/trace_gcd_crossed_coinvariant_norm_target.md
p24/trace_gcd_crossed_coinvariant_norm_toy.py
p24/trace_gcd_actual_cm_square_coinvariant_block_cycle_audit.py
p24/trace_gcd_two_resultant_holdout_audit.py
```

Question to answer:

```text
Can one transported square-map crossed norm be proved a p-unit without
enumerating the class set?
```

Do not collapse this to an ordinary base-polynomial equality; the holdout
shows that distinction is real.

## Selected-Chain Tower / Kummer Route

Read when working on the alternate certificate surface that constructs an
embedded quotient tower plus one selected recovery polynomial.

```text
p24/phase_lifted_tower_certificate_toy.py
p24/phase_chain_executable_frontier.md
p24/kummer_orbit_minpoly_producer_frontier.md
p24/tower_kummer_phase_complexity_boundary.md
p24/tower_kummer_phase_complexity_scan.py
p24/tower_kummer_glue_complexity_boundary.md
p24/tower_kummer_glue_complexity_scan.py
p24/relative_kummer_reconstruction_toy.py
p24/relative_kummer_orbit_norm_toy.py
p24/relative_kummer_multi_orbit_ambiguity_gate.md
p24/relative_kummer_multi_orbit_ambiguity_gate.py
p24/abstract_tower_fiber_map_boundary.md
p24/abstract_tower_morphism_payload_boundary.md
p24/lean/KummerCrossOrbitGlueGate.lean
p24/lean/PhaseLiftedTowerPayloadGate.lean
```

Question to answer:

```text
Can the embedded 157/211 relative tower be produced class-set-free?
```

Do not state this as "construct six 211-layer Kummer minpoly orbits" alone.
The p24 `211` layer has six primitive-character Frobenius orbits, and Kummer
powers leave `211^5` cross-orbit phase choices after global cyclic shift.
The producer must supply cross-orbit phase glue, selected child-polynomial
data, or the full relative morphism.  The finite glue gate shows invariants
`T_a/T_1^a` for the five non-base orbits restore child selection in small
multi-orbit models.

Fallback finite surface: if a producer constructs one honest oriented
recovery cycle for `2*463*223^(-1)` instead of a selected recovery
polynomial, the verifier can carry vertices and two path witnesses per edge:
`j_i --Phi_2-- u_i --Phi_463-- v_i <--Phi_223-- j_{i+1}`.  The p24 payload
is `9322325` field elements with `A,x0`, or `9589191` even if dense
`Phi_2/Phi_463/Phi_223` tables are serialized, both below `1e-5*sqrt(p)`.
This is larger than selected-chain and still needs the seedless cycle
producer, but it is a legitimate sub-sqrt landing surface if the arithmetic
construction naturally outputs a cycle.

## Centered P-Unit / Full-Origin Chow Route

Read when working on the compact base-field p-unit route for the centered
profile determinant product.

```text
p24/centered_profile_payload_frontier.md
p24/centered_profile_theorem_family_synthesis.md
p24/centered_marginal_phase_borcherds_target.md
p24/centered_marginal_full_origin_borcherds_gate.md
p24/centered_marginal_full_origin_phase_sensitivity_gate.md
p24/centered_marginal_full_origin_phase_sensitivity_gate.py
p24/centered_marginal_full_origin_edge_shape_boundary.md
p24/centered_marginal_full_origin_edge_shape_boundary.py
p24/centered_marginal_full_origin_path_shape_boundary.md
p24/centered_marginal_full_origin_path_shape_boundary.py
p24/centered_marginal_origin_product_theorem.md
p24/centered_marginal_origin_norm_power_theorem.md
p24/centered_marginal_origin_norm_power_audit.py
p24/centered_marginal_chow_integral_model.md
p24/centered_marginal_crossed_product_fitting_target.md
p24/centered_marginal_orbit_fitting_block_cycle_audit.md
p24/centered_marginal_orbit_fitting_block_cycle_audit.py
p24/lean/CenteredArcProductGate.lean
p24/lean/CenteredBorcherdsPUnitGate.lean
p24/lean/CenteredFullOriginBorcherdsGate.lean
```

Question to answer:

```text
Can a phase-aware class-field/Borcherds/Fitting section be constructed whose
CM value is the full-origin centered Chow product up to a p-unit?
```

Do not replace this by unordered recovery-fiber data.  The full-origin
phase-sensitivity gate preserves every unordered recovery fiber in the pinned
actual-CM row but changes the full-origin product in `8/8` controls.
Also do not assume a bounded one-edge modular correspondence formula: the
oriented-edge boundary finds no bidegree-`<=4` polynomial or rational formula
in `(j_i,j_{i+1})` for the same centered determinant sequence.
The short-path boundary extends this to two-edge and three-edge local path
coordinates: no polynomial/rational total-degree formula appears below the
generic interpolation threshold on the same row.
The orbit-Fitting block-cycle audit verifies the crossed-product determinant
line on the same actual-CM row; determinant assembly is sound, so the missing
input remains arithmetic p-unitness of that phase-aware Fitting section.

## Lean Gates

Read when formalizing an already-clear theorem statement.

```text
p24/lean/README.md
p24/lean/TraceGcdFullCoinvariantTailGate.lean
p24/lean/TraceGcdCrossedCoinvariantNormGate.lean
p24/lean/TraceGcdFrequencyDefectGate.lean
```

Use Lean for:

```text
dimension/count gates;
finite linear-algebra implications;
certificate-surface bookkeeping.
```

Do not use Lean yet for the block/skew Cauchy theorem until the actual
arithmetic operators `A,B` are identified.

## Harness And Verification

Read when adding/removing checks or updating counts.

```text
p24/trace_gcd_fast_falsifier_harness.py
p24/trace_gcd_useful_computation_strategy.md
```

Default verification:

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=p24 python3 \
  p24/trace_gcd_fast_falsifier_harness.py \
  --workers 4 --skip-spectral --no-danger3-inventory
```

Two-resultant verification:

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=p24 python3 \
  p24/trace_gcd_fast_falsifier_harness.py \
  --workers 4 --skip-spectral --no-danger3-inventory \
  --include-two-resultant-holdouts
```

If `py_compile` tries to write under `~/Library/Caches`, use:

```text
PYTHONPYCACHEPREFIX=/private/tmp/codex-pycache PYTHONDONTWRITEBYTECODE=1 \
  python3 -m py_compile <files>
```

## Actual-CM Data Boundaries

Read when checking whether smaller actual-CM rows can calibrate a theorem.

```text
p24/trace_gcd_actual_cm_full_plucker_chart_boundary.py
p24/trace_gcd_actual_cm_frequency_defect_boundary.py
p24/trace_gcd_actual_cm_rs_tail_semilinear_core_audit.py
p24/trace_gcd_actual_cm_gaussian_rs_tail_audit.py
p24/trace_gcd_actual_cm_square_coinvariant_audit.py
```

Current status:

```text
tail-only full-rank rows are too small/trivial for full-chart calibration;
tail-only full-rank rows are also not frequency-defect calibrations;
nontrivial prefix-plus-tail rows are singular controls;
prefix-plus-tail singular rows fail the local frequency-profile gate;
current small rows are useful as boundaries, not positive proof evidence for
the full block/skew chart.
```

## Files To Avoid By Default

Avoid loading broad historical families unless a new idea specifically needs
them:

```text
p24/axis_*.md
p24/trace_frame_*.md
p24/subagent_*.md
p24/agent_*.md
```

Those notes contain useful prior failures and boundaries, but they are not on
the current critical path unless the main route changes.
