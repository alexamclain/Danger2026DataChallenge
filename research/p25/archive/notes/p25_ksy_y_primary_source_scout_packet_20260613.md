# P25 KSY-y Primary-Source Scout Packet

Updated: 2026-06-13 21:03 PDT

## Purpose

The current moonshot is not broad CM/Lang exploration anymore.  It is a narrow
source-theorem hunt for the exact KSY-y product:

```text
P = prod_{j=-1..1} prod_{k=0..24} y(C+jD+kK)/y(-C-jD-kK)
C = (47,28)
D = (22,3)
K = (57,0)
y(Q) = -g(2Q)/g(Q)^4
```

Equivalent quotient anchor:

```text
C = (2,28)
D = (1,3)
T = -2C = (2,113)
T/2 = -C = (1,141)
```

The controlling classifier is:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_closing_theorem_obligation_gate.py
```

## Source Handles Checked

- Koo-Shin-Yoon, `Ray class fields generated by torsion points of certain
  elliptic curves`: <https://arxiv.org/abs/1007.2307>.  The abstract and
  introduction point to normalized `wp'` / y-coordinate ray-class generation
  through Theorem 5.3, plus y-coordinate class invariants in Theorem 6.2 and
  Corollary 6.4.
- Sprang, `Eisenstein-Kronecker series via the Poincare bundle`:
  <https://arxiv.org/abs/1801.05677>.  Section 5 constructs logarithmic
  derivatives of Kato-Siegel functions from the Kronecker section, including
  the even-`D` escape hatch that keeps the D=2 additive route alive.
- Kubert-Lang, `Units in the Modular Function Field IV. The Siegel Functions
  are Generators`: <https://eudml.org/doc/162977>, plus the book `Modular
  Units`: <https://link.springer.com/book/10.1007/978-1-4757-1741-9>.
- Schertz, `Construction of Ray class fields by elliptic units`:
  <https://eudml.org/doc/248002>, and Shin, `Generation of class fields by
  Siegel-Ramachandra invariants`: <https://arxiv.org/abs/1009.2253>.
- Official challenge/verifier surface:
  <https://github.com/AndrewVSutherland/DANGER3>.

## Artifact-Gated Scout Jobs

### 1. KSY Normalized-y Exact-P

Ask whether Koo-Shin-Yoon can be specialized or strengthened from ray-class
generation to an identity for the exact `P` above.

Positive payload:

```text
exact P, or exact finite-field value identity for P with mixed graph and period-156 context
```

First falsifier:

```text
source emits broad field generation, one torsion y-value, or a generic invariant
without the 75-atom mixed C_3 x C_169 product
```

Run on any candidate:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_source_claim_intake_gate.py \
  --candidate --name SOURCE_NAME --anchor ksy_normalized_y_siegel_formula \
  --output-kind divisor-additive --exact-product --mixed-graph --divisor-additive
```

Status:

```text
Koo-Shin-Yoon arXiv:1007.2307 has been scouted.
Equation (3.4) remains live as formula language.
Theorem 5.3 and Corollary 6.4 do not close exact P.
Marker: ksy_y_ksy_exact_p_primary_source_scout_rows=1/1
```

### 2. Kubert-Lang Mixed-Graph Product

Ask whether a Siegel-function generator theorem can emit the row-labeled
`C_3 x C_169` product, not merely pass the prime-power exponent screen.

Positive payload:

```text
exact row-labeled pairs, quotient reflection-center data, or raw equal-weight K-traced product
```

First falsifier:

```text
C169 projection, Kubert-Lang congruence hygiene, or modular-unit generation without the mixed row graph
```

Run on any candidate:

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_mixed_graph_obligation_gate.py \
  --candidate --name SOURCE_NAME --raw-product --base-row-anchor \
  --exact-row-labeled-pairs --reflection-center --arithmetic-producer
```

Status:

```text
Kubert-Lang EuDML doc 162977 / GDZ LOG_0038 has been scouted.
The source handle remains live as generator/exponent language.
Generator theorem, KL congruence hygiene, and C169 projection do not close.
Exact row-labeled pairs are only finite payload without an arithmetic producer.
Marker: ksy_y_kubert_lang_mixed_graph_primary_source_scout_rows=1/1
```

### 3. Sprang / Kronecker D=2 Additive Route

Ask whether the Kronecker-section construction gives a D=2-compatible
divisor/additive identity for exact `P`.

Positive payload:

```text
explicit even-D Kronecker/differential clause emitting exact P
```

First falsifier:

```text
ordinary Kato-Siegel Dtheta=2 claim, or confusion between KSY [2] and source Dtheta
```

Run on any candidate:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_source_parameter_hygiene_gate.py
```

Status:

```text
Sprang arXiv:1801.05677 has been scouted.
Even-D Kronecker/differential construction remains live.
Ordinary Kato-Siegel theta_D at D=2 remains rejected.
Distribution/dlog framework alone does not close exact P.
Marker: ksy_y_sprang_kronecker_d2_primary_source_scout_rows=1/1
```

### 4. Siegel-Robert / Siegel-Ramachandra Value Route

Ask whether the value-unit literature can give the exact finite-field value of
`P` with the period-`156` branch/root/telescoping context.

Positive payload:

```text
finite-field value identity for exact P, preserving mixed graph, with period-156 context
```

First falsifier:

```text
ambient period-780 value, generic class invariant, or value identity without the period-156 branch
```

Run on any candidate by comparing it to:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_period_value_upgrade_gate.py
```

Status:

```text
Schertz EuDML doc 248002 / Numdam JTNB 9 (1997), 383-394 has been scouted.
Shin arXiv:1009.2253 has been scouted.
Class-field generator language remains useful but does not close exact P.
Ambient period-780 values are rejected; exact values need period-156 context.
Marker: ksy_y_siegel_robert_period_value_primary_source_scout_rows=1/1
```

### 5. DANGER3 Framing / Extraction

Ask whether an exact finite-field identity is challenge-legal without being
treated as a forbidden CM shortcut, and whether it can be converted to a
concrete `(A,x0)` pair.

Positive payload:

```text
DANGER3 policy acceptance, accepted non-CM finite-field framing, or concrete A,x0 extraction
```

First falsifier:

```text
theorem-only progress with no finite-identity framing and no concrete triple
```

Run on any concrete triple:

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_submission_extraction_gate.py \
  --p P --A A --x0 X0
```

Status:

```text
AndrewVSutherland/DANGER3 remote HEAD a65658b7b194546957fa62f40d60ca63efc37f93 has been checked.
Official surface is concrete (p,A,x0) verification by vpp.py, with official lean_vpp.py for certificate generation.
Local p24-derived workspace has src/vpp.py but not src/lean_vpp.py.
Policy/theorem/extraction/submission states are separated.
Marker: ksy_y_danger3_framing_extraction_primary_source_scout_rows=1/1
```

## Post-Scout Reduction

The completed scout packet now reduces to a ranked moonshot queue:

```text
1. Continue first: Sprang/KSY exact theta2-or-P divisor/additive identity.
2. Continue lower: Kubert-Lang raw mixed product with arithmetic producer.
3. Continue value-side: Siegel-Robert exact P value with period-156 context.
4. Continue only on theorem hit or Drew answer: DANGER3 policy/extraction.
5. Kill as direct closers: broad class-field/generator shadows.
```

Local gate:

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_ksy_y_post_scout_moonshot_reduction_gate.py
```

Marker:

```text
ksy_y_post_scout_moonshot_reduction_rows=1/1
```

## Required Scout Output

Each scout returns at most one page:

```text
source(s) inspected:
exact theorem/lemma/equation numbers:
matched closing clauses:
first missing clause:
local gate command/probe:
continue/kill:
```

No broad relevance summaries.  A source that does not route through exact `P`,
the mixed graph, period-`156` value context, DANGER3 framing, or concrete
`vpp.py` extraction is killed for this moonshot.

## Completed Gate

```text
contract_rows                 = 5
exact_product_contracts        >= 3
value_contracts                >= 2
policy_or_extraction_contracts = 1
```

Local gate:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_ksy_y_primary_source_scout_packet_gate.py
```

Marker:

```text
ksy_y_primary_source_scout_packet_rows=1/1
```
