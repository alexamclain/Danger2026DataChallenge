# P25 KSY-y: Koo-Shin 2010 Full-Surface Screen

Updated: 2026-06-14 10:59 PDT

## Purpose

The supplied Koo-Shin 2010 paper is now screened beyond Theorem `5.2`.  The
result is useful, but as a boundary artifact: it gives integrality hygiene and
several generator/value surfaces, not the exact p25 product theorem.

## Source

```text
pdf  = /Users/agent/Downloads/s00209-008-0456-9.pdf
text = /Users/agent/Documents/Codex/pomerance-p25-run/incoming/extracted/s00209-008-0456-9.pdf.extract.txt
```

Verified surfaces in the extracted text:

```text
Theorem 3.9   prime-power orbit-sum integrality criterion
Theorem 5.2   prime-level constant-product rigidity and l-th-root descent
Theorem 6.2   complete one-axis X1(N) product/order formula
Theorem 8.1   K(X1(N)) generated by j and one Siegel product
Theorem 8.3   prime-level two-generator presentation
Theorem 9.8   CM/ray-class singular-value generator using a sum
Theorem 9.10  CM/ray-class singular-value generator using a product
Theorem 9.11  prime-level CM singular-value generator
```

## P25 Screen

Koo-Shin Theorem `3.9` gives a genuine positive check, but only a necessary
one:

```text
source_packet_common_level_507
  support = 6
  prime_power_terms = 2
  composite_terms = 4
  orbit_sums = ((169, (0, 1), 0),)
  preserves_mixed_graph = yes

theta2_inverse_raw_level_12675
  support = 300
  prime_power_terms = 4
  composite_terms = 296
  orbit_sums = ((169, (0, 1), 0),)
  preserves_mixed_graph = yes

c_axis_projection_level_169_control
  support = 6
  prime_power_terms = 6
  composite_terms = 0
  orbit_sums = ((169, (0, 1), 0),)
  preserves_mixed_graph = no
```

Interpretation: the live p25 packet and theta2 footprint pass the orbit-sum
integrality screen, but the `C_169` projection passes too after losing the
mixed graph.  So Theorem `3.9` is hygiene, not a selector.

Koo-Shin Theorem `6.2` is not the p25 producer:

```text
source_packet_common_level_507
  max_c_entries_per_right = 2 / 169
  max_right_entries_per_c = 1 / 3

theta2_inverse_raw_level_12675
  max_c_entries_per_right = 4 / 169
  max_right_entries_per_c = 25 / 75
```

Theorem `6.2` builds complete one-axis rows.  The p25 payload is sparse and
mixed: a `C_3 x C_169` packet, lifted to a 25-point kernel trace, with the
`T=(2,113)` quotient edge and theta2 orientation.  It cannot be obtained by
directly invoking the one-axis theorem.

## Verdict

Koo-Shin 2010 remains useful as:

```text
integrality/orbit-sum hygiene
constant-product rigidity
l-th-root descent context
CM/ray-class generator vocabulary
```

It is not a direct p25 closer unless upgraded to a mixed-level theorem that
selects exact `P`/theta2 with the `C_3 x C_169` row graph, `T` edge, equal
weights, and orientation.

## Gate

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_ksy_y_koo_shin_2010_full_surface_screen_gate.py
```

Marker:

```text
ksy_y_koo_shin_2010_full_surface_screen_rows=1/1
```
