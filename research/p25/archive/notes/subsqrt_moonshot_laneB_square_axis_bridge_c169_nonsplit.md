# p25 Lane B Square-Axis Bridge C169 Nonsplitting

Updated: 2026-06-12

## Result

The local C-axis source for the bridge is not a split `C_13 x C_13` object.  It
is the cyclic order-`169` subgroup of `F_677^*` generated by `p^2 = 354`.

The kernel of the visible projection `C_169 -> C_13` is generated by
`354^13 = 246` and consists of exponents `0,13,...,156`.  Every element of
order `13` lies in this kernel and projects trivially.  Conversely, every lift
of a nonzero visible `C_13` class has order `169`, and its `13`th power is the
nontrivial kernel element `g^(13a)`.

For the bridge factors:

- `D_c` has visible low class `3`, multiplier `85`, and `13`th-power kernel
  exponent `39` with value `383`.
- `T_c` has visible low class `9`, multiplier `667`, and `13`th-power kernel
  exponent `117` with value `365`.
- Both have C169 Kummer degree `169`; the cheaper degree-`13` data is only the
  shadow.

Thus the split no-carry fiber model is not merely the wrong combinatorial
choice; it is not a local multiplicative source for the bridge.  A producer
must use primitive cyclic `C_169` units, or an equivalent identity that encodes
the same nonsplit extension class.

## Command

```sh
PYTHONDONTWRITEBYTECODE=1 python3 research/p25/p25_laneB_square_axis_bridge_c169_nonsplit_gate.py
```

Expected row:

```text
square_axis_bridge_c169_nonsplit_rows=1/1
```

## Continue / Kill

Continue searching for an arithmetic realization of the 150-cell bridge, but
kill any candidate that treats the C-axis as independent order-`13` low/fiber
coordinates.  Candidates may use the `C_13` shadow diagnostically, but the
actual bridge edge has to live in the primitive cyclic `C_169` source.
