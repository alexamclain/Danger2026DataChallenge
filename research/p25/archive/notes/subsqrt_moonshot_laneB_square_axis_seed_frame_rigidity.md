# Subsqrt Moonshot Lane B Square-Axis Seed Frame Rigidity

Date: 2026-06-12

## Result

The seed-rigidity gate found exactly three `3 x 3` AP-rectangle completions
of the six-term no-borrow seed.  This gate classifies their frames.

Every oriented presentation uses only the signed directions generated by:

```text
Y     = 9
X     = 43
X + Y = 52
```

The `24` oriented presentations split as:

```text
{Y, X + Y}: 8 presentations
{Y, X}:     8 presentations
{X, X + Y}: 8 presentations
```

The three rectangle completions are exactly the three two-direction frames,
and every orientation is one of the four triangular no-borrow masks.

Script:

```text
/Users/agent/Documents/Codex/pomerance-p25-run/research/p25/p25_laneB_square_axis_seed_frame_rigidity_gate.py
```

Command:

```sh
cd /Users/agent/Documents/Codex/pomerance-p25-run
PYTHONDONTWRITEBYTECODE=1 python3 research/p25/p25_laneB_square_axis_seed_frame_rigidity_gate.py
```

Observed:

```text
square_axis_seed_frame_rigidity_rows = 1 / 1
```

## Consequence

The seed is not merely contained in a few accidental AP rectangles.  The
available AP frames are forced by the same `X/Y` coordinates used by the
group-ring normal form:

```text
S * (X + X^2 + X^2Y + X^3 + X^3Y + X^3Y^2)
```

This sharpens the producer contract:

```text
reject AP-rectangle explanations using unrelated directions;
accept only producers that recover the signed X/Y/X+Y frame geometry and the
triangular no-borrow masks.
```

The preceding checkpoint is:

```text
/Users/agent/Documents/Codex/pomerance-p25-run/research/p25/subsqrt_moonshot_laneB_square_axis_seed_rigidity.md
```
