# P25 Lane B: Robert KSY Kubert-Lang Primitive-D Word

Updated: 2026-06-13 16:45 PDT

## Purpose

The CRT-coupling gate proves that the Kubert-Lang packet is a mixed
`C_3 x C_169` graph lift.  This note rewrites the same packet in the primitive
cyclic coordinate generated by `D=(1,3)`.

This is a crosswalk, not a new producer.  It amortizes the earlier bridge
primitive-product rigidity work into the Kubert-Lang theorem search.

## Coordinate Change

In the common-level convention

```text
q = 169 * right + 3 * c  mod 507
```

we have:

```text
D=(1,3)       -> D_q = 178
D_q^-1        = 94 mod 507
T=(2,113)     -> T_q = 170 = 263 * D_q
base=(1,25)   -> q = 244 = 121 * D_q
```

Thus the source packet becomes:

```text
positive: z^121, z^122, z^123
negative: z^384, z^385, z^386
```

or, after shifting the base to zero:

```text
(1 + z + z^2) * (1 - z^263)
```

## Product Rigidity Inherited

The existing primitive bridge product-rigidity gate already enumerated all
normalized signed `2 x 3` products on the collapsed `C_507` word.  It found
only:

```text
forward = z^121*(1 + z + z^2)*(1 - z^263)
reverse = -z^384*(1 + z + z^2)*(1 - z^244)
```

So the Kubert-Lang packet is not a new short-product family.  It is exactly the
same rigid bridge word, expressed through the KSY/Robert quotient packet.

## Local Gate

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_primitive_word_gate.py
```

Expected marker:

```text
robert_ksy_theta2_kubert_lang_primitive_word_rows=1/1
```

## Interpretation

A theorem-side producer may target any of these equivalent finite forms:

```text
source graph packet in C3 x C169
primitive word z^121*(1+z+z^2)*(1-z^263)
KSY theta2 / theta2^-1 payload with the period-156 certificate
```

But it does not get to choose a different short product geometry.  The missing
moonshot step remains arithmetic legality: produce this rigid word with the
forced `K` trace and normalized-y/theta2 context.
