# P25 KSY-y Koo-Shin-Yoon Exact-P Scout

Updated: 2026-06-13 20:37 PDT

## Source Inspected

Koo-Shin-Yoon, `Ray class fields generated by torsion points of certain
elliptic curves`: <https://arxiv.org/abs/1007.2307>.

Primary clauses checked:

```text
Equation (3.4): normalized y/Siegel formula
Theorem 5.3: ray-class generation by torsion data
Theorem 6.2 / Corollary 6.4: y-coordinate class invariants
```

## Result

The source is useful, but it does not close the p25 moonshot.

Matched clauses:

```text
formula language:
  y(r1,r2)(tau) = -g(2r1,2r2)(tau) / g(r1,r2)(tau)^4

single-coordinate generation:
  K(N) generation from torsion-point coordinates when N >= 8 and 4 | N
  y(0,1/N)(theta)^4 generation under the Corollary 6.4 hypotheses
```

First missing clause:

```text
exact P = prod_{j=-1..1} prod_{k=0..24} y(C+jD+kK)/y(-C-jD-kK)
mixed C_3 x C_169 row graph / reflection-center payload
period-156 branch/root/telescoping context if the output is value-level
```

## Local Classification

```text
Equation (3.4) formula row:
  decision = conditional_missing_exact_product

Theorem 5.3 ray-class generation:
  decision = reject_not_closure_theorem

Corollary 6.4 single y-value:
  decision = conditional_missing_exact_product

hypothetical exact P value without period:
  decision = conditional_missing_period_156_context

hypothetical exact P divisor/additive identity:
  decision = closing_divisor_or_additive_identity
```

Local gate:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_ksy_y_ksy_exact_p_primary_source_scout_gate.py
```

Candidate probe matching the actual Theorem 5.3 shape:

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_ksy_theta2_kubert_lang_ksy_y_source_claim_intake_gate.py \
  --candidate --name ksy_theorem_5_3_generation_only \
  --anchor ksy_theorem_5_3_ray_class_generation \
  --output-kind field-generation
```

## Continue / Kill

Continue KSY only as a source for the normalized-y/Siegel formula vocabulary and
for possible follow-on product identities.

Kill `Theorem 5.3` or `Corollary 6.4` as direct closing theorems for p25: they
emit broad generation or a single y-value generator, not the exact mixed 75-atom
product `P`.

## Completed Gate

```text
formula_language_rows      = 1
direct_closing_rows        = 0
conditional_rows           = 3
rejected_rows              = 1
hypothetical_closing_rows  = 1
```

Marker:

```text
ksy_y_ksy_exact_p_primary_source_scout_rows=1/1
```
