# Subsqrt Moonshot Lane B Robert Bridge-Edge Quotient Contract

Date: 2026-06-13

## Result

The Robert/Siegel lane now has a finite divisor-quotient target, not only an
orientation requirement.

After the coupled positive layer is formed by the `K` trace and `D` segment,
the negative bridge layer is exactly the translate by the quotient edge

```text
U = (2, 113) in C_3 x C_169.
```

On the raw source axes `C_75 x C_169`, this edge has the 25 representatives

```text
(2,113), (5,113), ..., (74,113),
```

which are exactly the recorded bridge shift `T=(38,113)` plus the kernel trace
class generated by `K=(57,0)`.

## Checks

The gate verifies:

```text
positive support = 75
negative support = 75
quotient edge = (2,113)
quotient edge order = 507
raw recorded edge order = 12675
visible positive segment = (0,31), (1,25), (2,28)
visible negative segment = (0,138), (1,141), (2,144)
```

Both the quotient representative `(2,113)` and the recorded raw representative
`(38,113)` pass the bridge harness.  Pure C-side `(0,113)` and pure right-side
`(38,0)` edge controls fail the harness.

The same-row C differences are only:

```text
107 with multiplicity 25
116 with multiplicity 50
```

so the producer should not be modeled as a same-row C phase.

## Producer Consequence

A surviving Robert/Siegel candidate should produce a directed divisor, theta,
Miller-line, conjugate-unit, or differential quotient whose finite divisor edge
has source class `(2,113)` after the kernel trace is collapsed.

This complements the previous obstruction:

```text
not enough: x-only table
not enough: scalar-normalized C_169 character
not enough: same-row C-side sign tag
not enough: separated right-trace times C selector
needed: coupled K/D support plus the primitive edge quotient U=(2,113)
```

## Command

```sh
cd /Users/agent/Documents/Codex/pomerance-p25-run
PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_laneB_robert_bridge_edge_quotient_contract_gate.py
```

Script:

```text
/Users/agent/Documents/Codex/pomerance-p25-run/research/p25/p25_laneB_robert_bridge_edge_quotient_contract_gate.py
```
