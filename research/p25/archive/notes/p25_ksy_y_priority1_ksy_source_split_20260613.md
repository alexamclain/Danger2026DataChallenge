# P25 KSY-y Priority-1 KSY Source Split

Updated: 2026-06-13 21:40 PDT

## Purpose

Priority 1 keeps Koo-Shin-Yoon alive only through the normalized-y/Siegel formula
after it is upgraded to an exact p25 product theorem.  The same paper also has
ray-class generation and single-y-value invariant statements; those are useful
context, but they are not the K-traced anti-invariant product.

Primary source:

```text
Koo-Shin-Yoon, Ray class fields generated by torsion points of certain elliptic curves
arXiv:1007.2307
```

## Source Clauses

```text
Equation (3.4)
  role   = normalized-y/Siegel formula language y(Q)=-g(2Q)/g(Q)^4
  status = conditional_formula_language_without_product_proof
  missing = proof that the formula emits exact P over the p25 C,D,K atoms

Theorem 5.3
  role   = ray-class generation by torsion data
  status = reject_field_generation_not_product_identity
  missing = exact divisor/additive identity for P

Theorem 6.2 / Corollary 6.4
  role   = single normalized-y value / ray-class invariant statement
  status = conditional_missing_exact_product
  missing = exact P and mixed C_3 x C_169 graph selector
```

## Closing Shape

The KSY priority-1 lane closes only if a source theorem supplies:

```text
exact K-traced normalized-y product/distribution identity
or exact theta2/theta2^-1 divisor/additive data
for P = prod_{j=-1..1} prod_{k=0..24} y(C+jD+kK)/y(-C-jD-kK)
C = (47,28), D = (22,3), K = (57,0)
```

The exact-product hypothetical routes through exact-product intake as:

```text
closing_exact_product_identity
```

## Local Gate

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_ksy_y_priority1_ksy_source_split_gate.py
```

## Completed Gate

```text
formula_language_rows          = 1
generation_rejected_rows       = 1
single_value_conditional_rows  = 1
closing_hypotheticals          = 1
```

Marker:

```text
ksy_y_priority1_ksy_source_split_rows=1/1
```
