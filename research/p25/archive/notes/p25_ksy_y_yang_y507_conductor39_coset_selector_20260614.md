# P25 KSY-y Yang Y_507 Conductor-39 Coset Selector

Updated: 2026-06-14 10:05 PDT

## Result

The primitive conductor-`39` unit has a compact multiplicative-coset form:

```text
U_chi = -chi_3 * chi_13
      = 1_{7<2>} - 1_{<2>}       in (Z/39Z)^*
```

Here `<2>` is the cyclic subgroup generated by `2`:

```text
generator       = 2
generator_order = 12
kernel <2>      = (1, 2, 4, 5, 8, 10, 11, 16, 20, 22, 25, 32)
coset 7<2>      = (7, 14, 17, 19, 23, 28, 29, 31, 34, 35, 37, 38)
```

The coefficient layers match exactly:

```text
negative layer of U_chi = <2>
positive layer of U_chi = 7<2>
compact pair count      = 12
```

So a source theorem can target the cyclic quotient product

```text
prod_{h in <2>} E_{7h} / E_h
```

and then apply the already verified powers and Yang distribution:

```text
V_bal = 3 * U_chi
W     = 6 * U_chi
Norm_156(Y_507) = distribution_lift_39_to_507(W)
```

## Frobenius

For p25:

```text
p mod 39 = 23
23 in 7<2>
p swaps <2> and 7<2>
p^2 preserves both cosets
```

This gives a compact explanation of the anti-invariance:

```text
Frob_p(U_chi) = -U_chi
```

## Boundary

This is a source compression, not a certificate.  It still needs a theorem
that turns the coset quotient into a finite-field value/divisor identity, then
DANGER3 framing, extraction of `(A,x0)`, and official `vpp.py` verification.

## Local Gate

```sh
PYTHONPATH=research/p25 PYTHONDONTWRITEBYTECODE=1 python3 \
  research/p25/p25_ksy_y_yang_y507_conductor39_coset_selector_gate.py
```

Expected marker:

```text
ksy_y_yang_y507_conductor39_coset_selector_rows=1/1
```
