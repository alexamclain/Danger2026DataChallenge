# P25 Frontier

Updated: 2026-06-17

## Live Run

- [Practical search](lanes/practical-search.md) is the only concrete route to a
  certificate today.
- The active fleet is a 10-worker `x16halvenonsplit` run on
  `p = 10^25 + 13`. The private cockpit has a fresh 2026-06-17 smoke check
  showing the detached launcher layout, workers, watcher, and heartbeat alive,
  with no hit or exhausted marker. This public mirror records the operating
  shape but intentionally omits fresh heartbeat and trial-count telemetry.
- The previous production chunk died partway through. We are treating that as a
  partial unexplained termination, not a completed search region.
- The standard relaunch path is now the detached launcher plus heartbeat:
  it writes `LATEST_P25_RUN.txt`, `COMMAND.txt`, `pids.txt`, `watch.pid`, and
  `watch.log`; do not start a second manual watcher for launcher-managed runs.

## First-Pass Theorem Targets

- [H0](lanes/h0.md): source legality is real, and the legal objects have been
  compressed to four 78-over-78 H0/H0-translate products. The missing object is
  still a finite divisor/additive or period-156 value theorem with the H90
  boundary `Norm_156(Y_507)`.
- [Conductor 39](lanes/conductor39.md): `U_chi/V_bal/W` are legal source words,
  and the finite target has been compressed to a legal sparse Hilbert-90
  selector or canonical 78-over-78 Yang-fiber product with
  `(1 - Frob_p)` boundary `Norm_156(Y_507)`. The missing object is still the
  finite value/divisor theorem plus DANGER3 framing and extraction.

These two are the first-pass moonshot fronts because the source objects are
already certified and are cheaper than exact-P.

Every first-pass pass must end in either a source-stage closing candidate
matching the existing positive triage row, or a sharp falsifier that kills the
attempted theorem shape.

The latest unification pass shows that H0 and conductor 39 are two source
languages for the same four finite targets: support-156, 78-over-78
Yang/Hilbert-90 products with boundary `Norm_156(Y_507)`. H0 language asks for
an exact legal H0/H0-translate product; conductor-39 language asks for the
mixed `U_chi/W` object preserving `chi_3 tensor chi_13`. At the finite
value/divisor level, the ask is now one target family, not two independent
fronts.

The [arithmetic baseline audit](evidence/p25_v2_arithmetic_baseline_audit_20260616.md)
now verifies the constants behind that target: `p mod 8 = 5`, `k = 42`, the
three admissible traces and odd parts, conductor-4 trace discriminants, and
PARI `quadclassunit` class groups. This is the current arithmetic source of
truth for transfer-lane computations.

The [unified submission/extraction contract](evidence/p25_v2_unified_submission_extraction_contract_20260616.md)
now pins the downstream ladder after any theorem hit: DANGER3 framing,
same-`j` `X_1(8112)`, practical `X_1(16)` chart payload, 38 halving links or
direct `x0`, and official `vpp.py`. No current theorem payload satisfies any
submission-ladder row yet.

The [unified source-theorem gap](evidence/p25_v2_unified_source_theorem_gap_20260616.md)
now records that the H90/gauge/Yang/product-normal-form side is saturated.
There is no useful hidden selector freedom left in the first-pass target; the
missing object is a genuine finite arithmetic value/divisor theorem for one of
the four legal support-156 products.

The [H0 theorem-interface contract](evidence/p25_v2_h0_theorem_interface_contract_20260616.md)
is now cockpit-tracked as the compact H0 target. It validates the four legal
H0/H0-translate products, the two source exits (period-156 value theorem or
divisor/additive H90-boundary theorem), the period/root constraints
`gcd(4^156 - 1, p - 1) = 1` and ambient `mu_11`, and the same-`j`
`X_1(8112)` bridge payload. Current H0 source theorems remain zero; the page
exists to classify future H0 answers quickly.

The [conductor-39 Yang/H90 interface contract](evidence/p25_v2_conductor39_yang_h90_interface_contract_20260616.md)
is now cockpit-tracked as the compact conductor-39 target: mixed
`U_chi = -chi_3 * chi_13`, coset selector, `ord_39(p)=6`, Hilbert-90 boundary
`W=(1-Frob_p)V`, sparse level-507 Yang lift, four quotient representatives
`(1,2,4,8)`, and the 78-over-78 product normal form. It still has zero
source-stage closers; its purpose is to keep future conductor-39 answers aimed
at the exact finite value/divisor theorem.

The [conductor-39 doubling-orbit minimality checkpoint](evidence/p25_v2_conductor39_doubling_orbit_minimality_20260616.md)
also kills a tempting shortcut: the seed ratio `E_7/E_1` is valid only inside
the full 12-step doubling-orbit norm. All 27 proper suborbit norms fail as
standalone `X_1(39)` units.

The [unified value/divisor interface](evidence/p25_v2_unified_value_divisor_interface_20260616.md)
now names the actual source-stage close: a finite divisor/additive theorem, or
a period-156 value theorem with branch/root/telescoping context, for one legal
support-156 product with `Norm_156(Y_507)` boundary. Source-legality,
boundary-only, selector/gauge, seed/suborbit, ambient-value, and
finite-payload-only claims are repair or reject rows.

The [period-156 value branch contract](evidence/p25_v2_period156_value_branch_contract_20260616.md)
now makes the value-side falsifier mechanical: `ord_39(p)=6`,
`sqrt(-39) notin F_p`, `gcd(4^156-1,p-1)=1`, and
`gcd(4^780-1,p-1)=11`. Divisor/additive claims avoid this branch issue;
value claims must supply support-period-156 branch/root/telescoping context.
Ambient-period-780 value claims need repair; taking an 11th power or
quotienting by `mu_11` is still only a repair row because it does not select
one `F_p` value. Direct `F_p` order-39-root or `sqrt(-39)` shortcuts are
rejected.

The [period-156 value source hook](evidence/p25_v2_period156_value_source_hook_20260616.md)
is the compact Schertz/Shin/Scholl/Siegel-Robert intake rule. A value-side lead
must give an arithmetic finite value theorem for one exact oriented
support-156 edge, or an accepted period-156 theta2 payload with bridge data.
Class-field generation, ambient-period-780 values, 11th powers, `mu_11`
quotients, degree-6 values without descent, and direct Scholl `D=2` imports are
repair or reject rows. Current period-156 value theorem count remains zero.

The [external distribution-relation scout](evidence/p25_v2_external_distribution_relation_scout_20260617.md)
now adds the nearby modern Siegel-unit distribution/Eisenstein-class cluster
to the same boundary. Polylogarithmic Eisenstein distribution relations,
Beilinson-Kato modular-symbol distributions, Kato-Siegel theta functions, and
Kubert-Lang generator/distribution theory are support or repair rows unless
they emit one accepted p25 finite row, period-156 value, or exact-P/theta2
payload hook.

The [distribution relation closure screen](evidence/p25_v2_distribution_relation_closure_screen_20260617.md)
now gives the finite group-ring reason for that boundary. In the legal edge
basis `(m1,m2,m4,m8)`, standard vertex sums, opposite diagonals, and all-four
norms have even `W`-boundary scale, while quotient relations have boundary
zero. Their integer closure therefore cannot produce one boundary-`W` unit
edge. Reconstructing a row from aggregate plus quotient data requires division
by `2` or `4`, so it remains root/selector/scalar repair unless a source gives
a direct edge theorem or the missing normalization.

The [period-156 value candidate sweep](evidence/p25_v2_period156_value_candidate_sweep_20260617.md)
now consolidates the value-side near-misses. Canonical H0/Y507 value language,
ambient-period-780 values, `mu_11` quotients, degree-6 values, dense norm
identities, Schertz/Shin/Scholl framework sources, theta2 support contracts,
and Sprang D=2 machinery do not currently promote to a source-stage closer.
The surviving value asks are exact period-156 H0/Y507 value theorem,
scalar-fixed H0 divisor/additive identity, or exact theta2/theta2-inverse
payload with the period-156 bridge.

The [period-156 lookup-row status](evidence/p25_v2_period156_lookup_row_status_20260617.md)
turns the second source-lookup row into a compact ask. Two value hooks are live
but not in hand: canonical H0 finite value with `Norm_156(Y_507)` boundary and
period-156 branch data, or `Y_507` finite value with period-156 context and a
legal-row bridge. The branch-free H0 divisor/additive case routes back to
priority 1; theta2 is support until an exact payload appears; Schertz/Shin/
Scholl framework language needs a specialization theorem; ambient or shortcut
values stay repair/reject.

The [period-156 row bridge packet](evidence/p25_v2_period156_row_bridge_packet_20260617.md)
now turns that value-side route into six required clauses: finite `F_p` value
or divisor/additive payload, arithmetic source theorem, period-156 branch or
additive normalization, legal support-156 row bridge, `Norm_156(Y_507)`
boundary, and scalar/branch normalization over `F_p`. Canonical H0 value,
`Y_507` value with legal row bridge, and theta2 payload with period-156 bridge
are accepted packet shapes; missing row bridge, missing period branch,
ambient-780 values, degree-6 values without `F_p` row descent, norm-only
claims, and finite payloads without source remain repair or reject rows.
Current period-156 value packets and source-stage closers remain zero.

The [period-156 feasibility supersession](evidence/p25_v2_period156_feasibility_supersession_20260617.md)
now records that no extra broad feasibility layer is needed for this route.
The row-bridge packet already covers the three accepted packet shapes and the
ambient-780, degree-6, norm-only, and source-less repair rows. Continue only
on an exact arithmetic source theorem or new source data outside that packet.

The [Q/Yang lookup-row status](evidence/p25_v2_q_yang_lookup_row_status_20260617.md)
turns the conductor-39 Q/Yang/H90 lookup row into a compact ask. The live
forms are a direct mixed `U_chi/W` Yang/H90 finite theorem for one legal
support-156 row, `Q` or `Q^3` finite theorem data with selector debt paid, Q
diagonal plus correct pure quartic split plus oriented root/sign, exact
scalar-fixed Q-square value plus extraction map, or a direct one-edge theorem
routing back to priority 1. Local Koo-Shin/KSY/Koo-Shin II/Sprang source scans
remain negative for a Q-route hook; current Q source hooks and source-stage
closers remain zero.

The [Q/Yang row bridge packet](evidence/p25_v2_q_yang_row_bridge_packet_20260617.md)
now makes the conductor-39 Q support route packet-shaped. Source-stage
promotion requires a mixed Yang/H90 finite theorem, Q or Q^3 theorem data with
selector normalization to one oriented edge, or Q diagonal plus the matching
pure quartic split and oriented root/sign. Exact Q-square value is only an
extraction payload when it also comes with a map from the two row roots to
same-j/X_1(16)/halving data or concrete `(A,x0)` candidates. Q/Q^3 without
selector, Q^6 boundary-only, diagonal-only, split without oriented root,
Q-square without extraction map, local Q vocabulary, pure-character degree-6
norms, and direct `vpp.py` on row values remain repair or reject. Current Q
source hooks, source-stage closers, extraction-ready rows, and submission-ready
rows remain zero.

The [normalizer lookup-row status](evidence/p25_v2_normalizer_lookup_row_status_20260617.md)
turns the row/quartic/power normalizer lookup row into a compact ask. Row,
reciprocal, quartic, projector, and power language counts only if it
normalizes to one scalar-fixed legal support-156 row with the boundary or
accepted period-156 bridge and finite theorem data. Selector-only quartic
data, unordered or aggregate orbit data, projector components without selected
root, powered boundary-only statements, Q split without oriented root, and
Lane B quotient power relations remain repair/reject rows. Current normalizer
source-stage closers remain zero.

The [Schertz/Shin/Scholl external source boundary](evidence/p25_v2_schertz_scholl_external_source_boundary_20260616.md)
now records the web-source status for that value family. Schertz, Shin, and
Scholl are real anchors for ray-class/value-unit/theta distribution language,
but none currently emits a p25 period-156 H0/Y507 value theorem, scalar-fixed
divisor/additive identity, or accepted theta2 payload with branch/root or
additive normalization. Current source-stage closers remain zero.

The [H0/Y507 period-156 compatibility screen](evidence/p25_v2_h0_y507_period156_compatibility_20260616.md)
now pins the H0-specific version of that value route. Accepted source-stage
shapes are canonical H0 value with `Norm_156(Y_507)` boundary and period-156
branch context, `Y_507` value with period-156 context, or canonical H0
divisor/additive identity with boundary. Formal one-coset values, ambient
period-780 values, and finite payloads without arithmetic source theorem stay
repair or reject rows.

The expert-response rubric, source-snippet intake, and source-family router
now use that same H0/Y507 screen as the value-side front door. Generic
period-value language is not enough; it must name canonical H0 with boundary
or `Y_507` with period-156 branch context before it can count as a source-stage
candidate.

The [positive theorem clause matcher](evidence/p25_v2_positive_theorem_clause_matcher_20260616.md)
now makes the accepted side mechanical: five first-pass H0/conductor-39/H0-Y507
front-door routes and one exact-P heavy route are named by required clauses.
The added character-language route requires `W` boundary, exact
row-antisymmetric `C4_1` phase, mixed tensor row sign, oriented
row/boundary-sign convention, arithmetic source theorem, and scalar-fixed
finite divisor/additive identity. The new power-normalized route accepts exact
source theorems for one legal row with
`e in {3,5,13,39,75,169,507}` and the boundary/period bridge fixed. Source
legality, boundary-only, H0/Y507 value without period-156 context, ambient
`mu_11` value data, ambiguous power values, projector/two-edge values, coarse
quartic data, reciprocal phase with wrong boundary, finite payloads without
source, and exact-P vocabulary-only answers are repair or reject rows.

The [front-door count sync](evidence/p25_v2_frontdoor_count_sync_20260616.md)
keeps the handoff counts straight: the clause matcher has five first-pass
presentations after adding the power-normalized route; the expert rubric has
three direct row families plus a normalize-then-intake power row; the minimal
ask phrases the first-pass math as three yes/no questions plus the heavier
exact-P route and Q support variants. This is grouping, not a new theorem
discrepancy, and source-stage closers remain zero.

The [quartic selector candidate sweep](evidence/p25_v2_quartic_selector_candidate_sweep_20260617.md)
now audits the closest character/projector near-misses. Exact `C4_1` phase,
row-sign/C4 tensor data, reciprocal phase presentations, two-edge/projector
components, and Q diagonal-plus-split structure are all useful selector or
normalization artifacts, but none is newly promoted to a source-stage closer.
The surviving quartic ask remains the exact row-antisymmetric `C4_1` selector
plus mixed row sign, orientation/boundary convention, arithmetic source
theorem, and scalar-fixed finite value/divisor theorem for one legal row.

The [theta2 period-156 support contract](evidence/p25_v2_theta2_period156_support_contract_20260616.md)
now makes that accepted theta2 payload more concrete. The finite side has a
factor-level period-156 certificate on `C_75 x C_169` and accepts theta2 or
theta2-inverse divisor/additive data, but current arithmetic producers and
source-stage closers remain zero. Multiplicative value-only unit claims still
have an 11-branch ambiguity unless a source supplies branch/root/telescoping
or divisor/additive normalization.

The [Sprang theta2 source intake](evidence/p25_v2_sprang_theta2_source_intake_20260616.md)
now sharpens the D=2 source status. Sprang is real support for
Poincare/Kronecker/theta machinery and avoids the classical coprime-to-6
obstruction at the logarithmic-derivative level, but the local arXiv sources
still do not emit the exact p25 theta2 divisor/additive payload, compact KSY
packet, `Norm_156(Y_507)` bridge, or branch-selection data.

The [Sprang distribution instantiation falsifier](evidence/p25_v2_sprang_distribution_instantiation_falsifier_20260616.md)
rules out the most tempting over-credit: Sprang's distribution theorem is a
full kernel/torsion trace identity, not the sparse p25 quotient packet or
`base*K_trace*D_segment*(1-T)` factor tuple. A Sprang-based closer still needs
that sparse selector and orientation.

The [Kubert-Lang selector boundary](evidence/p25_v2_kubert_lang_selector_boundary_20260616.md)
now makes the exact-P/KL side equally precise. The exact packet passes the
necessary KL exponent screen, the primitive word is the rigid bridge
`z^121 * (1 + z + z^2) * (1 - z^263)`, and the quotient `C_3 x C_169`
selector is rigid after all `257049` center/D choices are scanned. This is a
positive finite boundary, not a source closer: current KL source theorems
emitting that packet remain zero.
The [Kubert-Lang external source boundary](evidence/p25_v2_kubert_lang_external_source_boundary_20260616.md)
now records the web-source status: KL IV/`Modular Units` and modern summaries
are real anchors for generator and congruence vocabulary, but broad KL
generator theory is not the p25 hook. A useful KL lead must emit the rigid
primitive word above, the row-labeled mixed `C_3 x C_169` selector with
orientation, or an accepted theta2/theta2-inverse payload. Current KL
source-stage theorems remain zero.

The [KL primitive-word source split](evidence/p25_v2_kl_primitive_word_source_split_20260617.md)
turns that rigid word into a more source-facing exact-P hook. The normalized
word factors as `(1+z+z^2)*(1-z^263) = (1-z^3)*(1-z^263)/(1-z)`, and the same
bridge is recovered from the three-term Hilbert-90 chain `-(1+z+z^-121)` with
unique primitive boundary step `122` up to sign. These are accepted only with
orientation, raw K-trace/theta2 period-156 bridge data, and an arithmetic
source theorem; generic KL generator language remains support.

The [KL source-split local scan](evidence/p25_v2_kl_source_split_local_scan_20260617.md)
applies that sharper hook to the raw local source corpus. KSY, Sprang,
Koo-Shin 2010, and Koo-Shin II contain helper vocabulary, but none emits the
exact oriented primitive word, three-term H90 source chain, boundary step, or
K-trace/theta2 bridge as a source theorem.

The [exact-P/theta2 producer obstruction](evidence/p25_v2_exactp_theta2_producer_obstruction_20260617.md)
now packages the heavy route as six positive producer rows and six falsifier
rows. A future exact-P update must supply an arithmetic theorem for the
compact `C,D,K` branch, equal-weight 75 atoms, theta2 divisor/additive
payload, KL primitive/H90-chain hook, Sprang sparse specialization, or
explicit reverse reconstruction. Finite selectors, source vocabulary,
value-only theta claims, distribution traces, and unified-target recovery
claims remain support or repair. Current exact-P/theta2 source producers and
source-stage closers remain zero.

The source-snippet intake and expert-response rubric now consume the
[exact-P minimal hook](evidence/p25_v2_exactp_minimal_hook_20260616.md)
directly. KSY normalized-y vocabulary, raw Kubert-Lang exponent balance, wrong
`C,D,K` packets, branchless orientation words, theta2 values without
period-156 branch context, nonuniform or missing atom weights,
ambient-period-780 value-only claims, and unified-target-as-exact-P recovery
claims are now named repair or reject rows rather than loose exact-P
possibilities.

The [exact-P candidate sweep](evidence/p25_v2_exactp_candidate_sweep_20260617.md)
now consolidates the heavy-route near-misses. KSY normalized-y vocabulary,
finite 75-atom fixtures, compact `C,D,K` packet data, Kubert-Lang exponent and
primitive-word screens, theta2 period-156 support, Sprang D=2 support, value
bridges, and unified-target theorems do not currently promote to an exact-P
source closer. The surviving exact-P asks are a compact `C,D,K,orientation`
theorem with one accepted branch, exact equal-weight 75-atom theorem, accepted
theta2/theta2-inverse divisor-additive payload with period-156 bridge, or an
explicit reverse reconstruction theorem.

The [exact-P orientation branch router](evidence/p25_v2_exactp_orientation_branch_router_20260616.md)
now makes the compact word `orientation` operational: four raw center/reverse
branches are accepted, two emitting `theta2^-1` with recovered sign `-1` and
two emitting `theta2` with recovered sign `1`. Branchless orientation language
is repair; wrong center, wrong `D`, or nonprimitive `K` is rejected; value-only
branch claims still need period-156 theta2 context.
The minimal hook, source-snippet intake, and expert-response rubric now all
consume this branch router.

The [degree-6 value descent ambiguity](evidence/p25_v2_degree6_value_descent_ambiguity_20260616.md)
now separates useful degree-6 value language from a source-stage close:
primitive 13th and 39th roots first occur over degree 6, so a value/orbit over
`F_{p^6}` is repair unless it explicitly descends to `F_p` and selects one
legal support-156 row, or supplies an equivalent Hilbert-90 ratio boundary.

The [value/divisor source-family router](evidence/p25_v2_value_divisor_source_family_router_20260616.md)
turns that interface into a literature rule: the first-pass ask is the unified
divisor/additive theorem, the character-language support route is exact
`C4_1` selector plus mixed row sign, orientation/boundary-sign data, and the
same finite theorem, the only support value route is period-156 with
branch/root/telescoping context, and exact-P stays heavy unless a source
already promises the 75-atom theorem or the `75 -> 300 -> 12 -> 312 -> 156`
bridge.
No broad source-family reread is currently justified.

The [source-action registry](evidence/p25_v2_source_action_registry_20260616.md)
now makes that operational. The only live source actions are theorem-shaped:
the first-pass divisor/additive theorem, the period-156 H0/Y507 value theorem,
or the heavy exact-P/theta2 theorem. Koo-Shin retrieval is closed; Sprang,
Kubert-Lang, conductor-39 Q, twisted-H90, and curved-corner are support-only
unless a snippet already carries one of the accepted hooks.

The [first-pass expert intake packet](evidence/p25_v2_first_pass_expert_intake_packet_20260616.md)
is now supporting classifier provenance for Drew/source feedback. The current
expert-facing handoff is the Drew kernel review packet, which carries the
expanded unique-power set and exact-P 75-atom separation. The older intake
packet still explains the clause structure for first-pass H0/conductor-39/H0-
Y507 rows and the heavier exact-P/theta2 upstream bridge. Source legality,
boundary-only, selector-only, aggregate, Q-only, ambiguous power, and
branchless value answers remain immediate repair or reject rows.

The [external H90 literature scout](evidence/p25_v2_external_h90_literature_scout_20260616.md)
keeps that rule after a narrow web pass. Five superficially relevant external
sources had Hilbert-90, Siegel-unit, Kubert-Lang, or theta vocabulary, but none
emitted the exact level-507 support-156 row, `Norm_156(Y_507)` boundary, or
scalar-fixed finite theorem. Broad Hilbert-90 quotient-map searches are now a
kill row unless they name the p25 row data.

The [unified theorem review packet](evidence/p25_v2_unified_theorem_review_packet_20260616.md)
is now the compact expert handoff: it lists the four exact product rows, the
accepted divisor/additive, exact quartic-character, and period-156 value
routes, six review questions, and the stop signs, including additive
normalization, coarse quartic character data, and the Theorem 5.2
constant-span obstruction.
Use it when asking whether the remaining gap is a known finite
divisor/additive identity, exact character-selected finite theorem, period-156
value theorem, or a genuinely missing arithmetic result.

The [unified group-ring payload](evidence/p25_v2_unified_group_ring_payload_20260616.md)
pins the algebraic object underneath that ask: level `507`, conductor `39`,
support period `156`, `W=(1-Frob_p)V`, period norm support `312`, and four
support-156 products of the form `prod E_{a+39k}^6 / prod E_{b+39k}^6`.

The [self-contained theorem statement](evidence/p25_v2_self_contained_theorem_statement_20260616.md)
is now the shortest expert handoff. It restates the exact four product rows,
row hashes, quotient-`C4` source graph, edge-lattice intake rule, three
accepted source-stage presentations, fourteen stop signs, and the downstream
non-submission boundary without requiring a reader to open the gate forest.

The [source graph normal form](evidence/p25_v2_source_graph_normal_form_20260616.md)
is the current shortest source-language bridge: the four legal rows are exactly
the four oriented edges of a quotient-`C4` `K_{2,2}` graph from odd cosets to
even cosets. A theorem for one edge is live; vertex, projection, quotient,
diagonal, row-square, or all-four aggregate statements are repair/reject unless
they select one oriented `W`-boundary edge.

The [orbit tuple theorem router](evidence/p25_v2_orbit_tuple_theorem_router_20260616.md)
keeps one useful exception precise: a row-labeled theorem for all four
doubling-orbit rows is a positive shape because it contains a theorem for one
exact edge. An unordered four-value set, symmetric all-four product/norm,
diagonal pair, row quotient, or orbit legality statement is still repair
unless it selects one scalar-fixed oriented edge.

The [K22 automorphism quotient falsifier](evidence/p25_v2_k22_automorphism_quotient_falsifier_20260616.md)
now makes the symmetry version of that rule explicit. Every nontrivial
side-preserving automorphism quotient of the legal four-edge `K_{2,2}` graph
has only two-edge or all-four orbits. Product aggregates therefore carry
`2W` or `4W` boundary, and quotient relations have zero boundary; neither is a
source-stage closer without root/scalar/selector data or a direct row-labeled
one-edge theorem.

The [edge lattice intake classifier](evidence/p25_v2_edge_lattice_intake_classifier_20260616.md)
now closes a broader over-crediting trap: in the edge basis the boundary scale
is the coefficient sum, so a non-edge combination can still have `W` boundary.
Such a vector is one edge plus nonzero boundary-zero lattice content, and is
repair unless the source also supplies that boundary-zero value, a selector, or
the direct one-edge theorem.

The [edge lattice global minimality](evidence/p25_v2_edge_lattice_global_minimality_20260616.md)
now makes that exact rather than bounded: over all integral edge combinations
with `W` boundary, the only `L1`-minimal vectors are the four unit edges.
Every other `W`-boundary vector has `L1 >= 3` and carries nonzero boundary-zero
selector debt.

The [zero-lattice transfer contract](evidence/p25_v2_zero_lattice_transfer_contract_20260616.md)
now pins how that boundary-zero debt can still be useful. Exact values for row
quotients or a rank-3 zero-lattice basis are transfer data: they can move an
already-known row theorem around the four legal edges, or normalize a nonunit
`W`-boundary theorem if the zero-lattice value is also known. They cannot
create the first absolute `Norm_156(Y_507)` row value by themselves.

The [zero-lattice candidate sweep](evidence/p25_v2_zero_lattice_candidate_sweep_20260617.md)
now audits the quotient/diagonal/row-square near-misses as a group.
Boundary-zero quotients, nonunit `W`-boundary vectors, diagonal aggregates, row
squares, two-edge pairs, Q diagonal data, and Q square values are support or
normalization artifacts, not hidden source-stage closers. They promote only
with exact boundary-zero values plus a real unit-edge theorem, or with explicit
oriented root/sign data and the same finite theorem data.

The [row-value reconstruction basis](evidence/p25_v2_row_value_reconstruction_basis_20260617.md)
now makes that support structure constructive. One scalar-fixed legal row
value plus the three boundary-zero quotient values reconstructs all four row
values. The three quotient values alone have coefficient sum zero and determine
only ratios; diagonal-plus-split data reaches `2*row` and still has the
oriented-root/sign debt. Thus the missing finite anchor is exactly one
absolute scalar-fixed row theorem or an equivalent theorem carrying that root
and normalization data.

The [common-scalar anchor filter](evidence/p25_v2_common_scalar_anchor_filter_20260617.md)
now turns that into a first-pass arithmetic screen. An exact finite theorem
fixes the common row scalar only when its coefficient sum, or its period branch
exponent, is invertible modulo `p - 1`. Quotient/zero-lattice data has
coefficient sum zero and can never be the first anchor; pair, projector,
Q-square, sixfold, `mu_11`, and ambient-period-780 answers retain specific
root or branch debt. Direct one-edge values, row-labeled unit-power values
including powers `75`, `169`, and `507`, and support-period-156 values remain
the scalar-fixing shapes if a source theorem supplies the exact finite payload.

The [basis-sensitive anchor sieve](evidence/p25_v2_basis_sensitive_anchor_sieve_20260617.md)
now orders all current intake filters. First identify the object basis; then
apply the row-lattice selector, common-scalar gcd, or period-branch test.
Coefficient-sum-one nonunit row vectors still need zero-lattice repair,
row-labeled unique powers can normalize by inverse exponent, period-156 values
need the legal-row bridge, and exact-P atom counts are heavy upstream payloads,
not row-power shortcuts.

The [affine row-product classifier](evidence/p25_v2_affine_row_product_classifier_20260617.md)
now covers aggregate products over the four legal rows. A row-affine product
with unit coefficient sum is not automatically a source-stage theorem: if it
is not already a row or row-labeled power, it normalizes to the current kernel
only when the exact matched zero-lattice quotient value is also supplied. Zero-sum
quotients remain transfer-only, and nonunit coefficient sums retain root or
selector debt.

The [affine row normal form](evidence/p25_v2_affine_row_normal_form_20260617.md)
now makes that aggregate intake deterministic. For any exponent vector `v`
and target row `e_m`, the matched quotient debt is
`z_m = v - (sum v)e_m`; exact values for `R^v` and `R^z_m` recover
`R_m^(sum v)`. Unit sums normalize to the current theorem kernel, zero sums
remain transfer-only, and nonunit sums retain root/scalar debt.

The [matched-quotient closure packet](evidence/p25_v2_matched_quotient_closure_packet_20260617.md)
promotes the precise aggregate repair package: an arithmetic theorem for
`R^v` plus an arithmetic theorem for the matched zero-lattice value
`R^(v - (sum v)e_m)` recovers `R_m^(sum v)`. If `gcd(sum v,p-1)=1`, this is a
current-kernel row theorem after inverse exponent. Aggregate-only,
quotient-only, unmatched quotient, and nonunit-sum packets remain repair or
transfer rows. Current matched-quotient source packets and source-stage
closers remain zero.

The [matched-quotient source feasibility screen](evidence/p25_v2_matched_quotient_source_feasibility_20260617.md)
now separates standard distribution aggregates from actually closing affine
packets. Pair/diagonal distribution aggregates with matched quotients recover
`R_m^2`, and the all-four norm recovers `R_m^4`, so they still carry square
or fourth-root debt. The genuinely closing aggregate shape needs a
nonstandard affine packet with exact `R^v`, exact matched
`R^(v - (sum v)e_m)`, and `gcd(sum v,p-1)=1`; no current source emits that
paired packet.

The [matched-quotient burden audit](evidence/p25_v2_matched_quotient_burden_audit_20260617.md)
now makes that support route harder to over-count. The three accepted affine
packets are equivalent to direct row data plus exact zero-lattice quotient
data; they normalize future answers but do not lower the source-theorem
burden unless a source independently supplies both arithmetic theorems.
Standard pair/norm packets still stop at `R_m^2` or `R_m^4`.

The [matched zero-lattice target audit](evidence/p25_v2_matched_zero_lattice_target_audit_20260617.md)
sharpens the remaining support ask. The viable matched-affine packets need
only two zero-lattice lines up to sign: exact `R1/R2` or `R2/R1`, and the
exact product `q2_1*q4_1*q8_1`. Generic quotient vocabulary, zero-boundary
divisor data, or a full unspecialized zero-lattice basis still does not change
source-stage status.

The [edge projector denominator](evidence/p25_v2_edge_projector_denominator_20260616.md)
adds the character-basis version of the same warning. The constant, row,
column, and checkerboard projectors reconstruct `4*edge`; only the constant
component carries boundary `4W`, and p25 has `gcd(4,p-1)=4`. Projector or
character-component values are therefore repair unless they include an explicit
selected fourth root/scalar or a direct one-edge theorem.

The [partial projector selector](evidence/p25_v2_partial_projector_selector_20260616.md)
now covers the two-edge middle case: every row-pair, column-pair, or
diagonal-pair aggregate has boundary `2W`; every pair difference has boundary
zero; pair plus difference reaches `2*edge`, which still needs an oriented
square root because `gcd(2,p-1)=2`.

The [Yang lift / descent boundary contract](evidence/p25_v2_yang_lift_descent_boundary_contract_20260616.md)
now makes the conductor-39 promotion ladder explicit: mixed `U_chi/W` source
word, level-507 Yang lift, Hilbert-90 descent with `Norm_156(Y_507)` boundary,
then finite value/divisor theorem. Anything stopping before the finite theorem
is repair; projection, suborbit, altered-lift, or wrong-boundary variants are
rejected for the current target.

The [conductor-39 norm-one quotient route](evidence/p25_v2_conductor39_norm_one_quotient_route_20260616.md)
adds a smaller value-side support object:
`Q = prod_{h in <2>} E_{7h} / E_h`, with `Frob_p(Q)=Q^-1` and
`Q^6=(1-Frob_p)(Q^3)`. A theorem for `Q` with period-156 context, or for the
`Q^3` Hilbert-90 preimage plus finite value/divisor data, routes back into the
period-156/source-snippet intake. A source answer that only norms the pure
conductor-39 character down from degree 6 is rejected because that norm
cancels.
The source-snippet intake and current expert-response rubric now consume the
same `Q` rows, so future expert answers are classified consistently as support,
repair, or reject before any lane promotion.
The [Q-route selector debt screen](evidence/p25_v2_q_route_selector_debt_20260616.md)
adds the missing caution: `Q` value or `Q^3` H90 theorem data is still only a
support route unless it also recovers one oriented quotient-`C4` edge, supplies
the boundary-zero value needed to normalize to one edge, or directly proves the
one-edge finite theorem.
The [Q diagonal normalization screen](evidence/p25_v2_q_diagonal_normalization_20260616.md)
now makes that selector debt explicit at projection level:
`Q_antisym=(-6,6,-6,6)=m1+m4=m2+m8`. Thus `Q` sees the diagonal aggregate, not
one edge. A `Q` theorem needs a boundary-zero split plus oriented root,
explicit oriented diagonal-split normalization, or a direct one-edge theorem
before it can leave support/repair status.
The source-snippet intake, expert-response rubric, and minimal expert ask now
consume these Q-diagonal cases directly: Q-only value data is support, Q plus
a row quotient is square-root repair, Q plus explicit oriented split
normalizes to one-edge intake, and wrong zero-boundary splits are rejects.
The [Q split quotient complexity screen](evidence/p25_v2_q_split_quotient_complexity_20260616.md)
now kills the easiest-looking split shortcut: the Q-relevant splits `m1-m4`
and `m2-m8` are support-24, all-twelve-column, boundary-zero quotients. The
smaller support-12 row quotients are real but are not the diagonal splits Q
needs.
The [Q split quartic-selector screen](evidence/p25_v2_q_split_quartic_selector_20260616.md)
adds the useful positive side: the Q diagonal aggregate is pure quadratic in
quotient-`C4` Fourier coordinates, while the matching Q splits `m1-m4` and
`m2-m8` are pure quartic selector data. Diagonal plus the correct split
recovers twice one legal edge; without oriented root/sign data or a direct
one-edge theorem, this remains repair/support rather than a source-stage
close.
The [Q square payload router](evidence/p25_v2_q_square_payload_router_20260616.md)
now separates that blocker into two cases. If a source gives an exact
scalar-fixed finite `F_p` value for the Q square, the row-value payload is
bounded to two square roots. If it gives only divisor, boundary, phase, or
value-up-to-scalar data, the route is still repair; the constant sign is
invisible to those invariants.
The [Q square extraction boundary](evidence/p25_v2_q_square_extraction_boundary_20260616.md)
now prevents overclaiming: those two row roots are not two `vpp.py` candidates.
They still need DANGER3 framing plus a same-j/X_1(16)/halving extraction map,
or a direct map to concrete `(A,x0)` candidates.
The [Q-route source-hook scan](evidence/p25_v2_q_route_source_hook_scan_20260616.md)
now applies this route to the local Koo-Shin, KSY, Koo-Shin II, and Sprang
sources. It finds no conductor-39 `Q` hook: no `E_7/E_1` orbit-norm theorem,
no `Q^3/Q^6` finite H90 theorem, no Q diagonal plus pure quartic split, no
oriented root/sign data, and no `Norm_156(Y_507)` or period-156 branch data.
Generic `Q` and generic splitting language are collision rows.
The [Q-route candidate sweep](evidence/p25_v2_q_route_candidate_sweep_20260617.md)
now consolidates those near-misses. `Q` value data, `Q^3` H90 data, `Q^6`
boundary data, diagonal aggregates, wrong support-12 splits, correct splits
without oriented root, Q-square values without extraction, and local generic
`Q`/split source language do not promote to a source-stage closer. The Q route
remains live only as support/normalization: Q or Q^3 finite theorem data
with selector debt paid, Q diagonal plus correct pure quartic split plus
oriented root/sign, exact Q-square value with extraction map, or direct
one-edge finite theorem.
The [Q/Yang lookup-row status](evidence/p25_v2_q_yang_lookup_row_status_20260617.md)
compresses that into the active conductor-39 ask and prevents generic
Q/Yang/H90 language from being over-counted. It accepts only direct mixed
Yang/H90 finite theorem data, Q/Q3 theorem data with selector debt paid,
Q diagonal plus correct pure quartic split plus oriented root/sign, Q-square
value plus extraction map, or direct one-edge theorem. Current Q source hooks,
source-stage closers, extraction-ready rows, and submissions remain zero.
The [Q-route consistency audit](evidence/p25_v2_q_route_consistency_audit_20260617.md)
now guards that boundary across the conductor-39 support artifacts: `Q/Q^3`
stays support until selector debt is paid, `Q^6` stays boundary-only repair,
and Q-square values remain bounded row payloads rather than source-stage or
extraction-ready closers.
The source-snippet intake, current expert-response rubric, minimal expert ask,
and frontdoor count sync now consume that Q-square payload distinction:
exact scalar-fixed Q-square value is a bounded row-value payload with
extraction-map repair, not a source-stage closer or extraction-ready payload,
while value-up-to-scalar and sign-from-invariants claims are repair/reject rows.

The [row orbit normalizer](evidence/p25_v2_row_orbit_normalization_20260616.md)
now records exactly how those four rows collapse under the doubling subgroup:
`(1,16,22)->m=1`, `(2,5,32)->m=2`, `(4,10,25)->m=4`, and
`(8,11,20)->m=8`. One normalized legal row is enough for the first-pass source
ask; units outside the doubling subgroup are not presentations of the current
target.

The [row orientation / reciprocal normalizer](evidence/p25_v2_row_orientation_reciprocal_normalizer_20260616.md)
refines that last clause: all 12 outside-doubling units are reciprocal
presentations of legal rows. They are not accepted as oriented rows unless the
source supplies explicit reciprocal orientation and the opposite
`-Norm_156(Y_507)` boundary sign, or rewrites to an oriented legal row.

The [row/orientation candidate sweep](evidence/p25_v2_row_orientation_candidate_sweep_20260617.md)
now closes the "maybe a row artifact is already enough" check. It promotes no
old candidate to source-stage closer. The surviving intake families are a
single normalized legal row, a doubling-equivalent row normalized to one legal
row, a reciprocal row with explicit `-Norm_156(Y_507)` boundary, or a
row-labeled four-row/parametric orbit theorem containing one legal row.

The [repair-debt closure matrix](evidence/p25_v2_repair_debt_closure_matrix_20260617.md)
now puts p25 root and scalar debt in one table. Five theorem shapes normalize
uniquely at source intake: direct one-edge theorem, row-labeled orbit theorem,
reciprocal-minus-boundary row, exact row-labeled `R_m^e` value for
`e in {3,5,13,39,75,169,507}`, and support-period-156 value theorem.
Ambient-period values carry `mu_11` debt, projector/four-edge data carries
`mu_4` debt, pair/square/Q-square data has two-root debt, divisor/H90
up-to-scalar data has full `F_p^*` scalar debt, and zero-lattice data remains
transfer-only.

The [source-stage normalization spine](evidence/p25_v2_source_stage_normalization_spine_20260617.md)
turns the accepted presentations into one intake normal form. Direct edge,
quartic-selector, row-labeled orbit, reciprocal-minus-boundary,
bijective-power, and support-period-156 value answers all reduce to one
scalar-fixed legal support-156 row if their required clauses are present.
Exact-P remains the heavy upstream bridge; projector, ambient-period,
pair/square, zero-lattice, up-to-scalar, and source-less finite payload rows
stay repair until their debt is paid.
The minimal expert ask and first-pass expert intake packet now consume this
spine directly, so expert/source replies are judged by whether they normalize
to that row before they change H0, conductor-39, or exact-P status.

The [route-priority/falsifier matrix](evidence/p25_v2_route_priority_falsifier_matrix_20260617.md)
now orders the remaining asks: direct scalar-fixed divisor/additive theorem
first, support-period-156 value theorem second, exact power/quartic/row-labeled
or reciprocal presentations only as normalizers, `Q` only as support, and
exact-P only as the heavy upstream route with an exact theorem hook.

The [priority-1 divisor/additive work order](evidence/p25_v2_priority1_divisor_additive_work_order_20260617.md)
now isolates the top ask: one challenge-legal arithmetic theorem producing a
finite scalar-fixed divisor/additive identity for one normalized legal
support-156 row with `Norm_156(Y_507)` boundary. Koo-Shin 2010 remains source
legality/context; Theorem 5.2 constant-span repair is killed for the legal
quotient-`C4` rows.

The [priority-1 candidate sweep](evidence/p25_v2_priority1_candidate_sweep_20260617.md)
now closes the "maybe already enough" check for this top route. Existing
source-snippet and expert-rubric accepted rows are classifiers, Koo-Shin
clauses are legality/context, row/quartic artifacts are normalizers, and
zero-lattice/Q/finite-payload artifacts carry repair or source-theorem debt.

The [priority-1 source lookup capsule](evidence/p25_v2_priority1_source_lookup_capsule_20260617.md)
turns that result into the next literature/expert checklist. A useful source
hit must be one of six named hooks: Koo-Shin H0/conductor-39 divisor/additive,
H0/Y507 support-period-156 value, conductor-39 Q/Yang/H90 with selector debt
paid, row/quartic/power normalizer with a finite theorem, Kubert-Lang exact-P
upstream, or Sprang theta2 sparse packet. Current priority-1 source theorems,
source-stage closers, and submissions remain zero.

The [priority-1 packet fixture contract](evidence/p25_v2_priority1_packet_fixture_contract_20260617.md)
now makes those lookup rows executable against the archived source-answer
fixtures. Under v2, only H0 and conductor-39 divisor/additive packets are
current first-pass positives; twisted-H90 and curved-corner packets are
support-only positives requiring a concrete period-156 snippet. Current packet
source theorems, source-stage closers, and submissions remain zero. The
contract is now bound to the four legal row hashes, so future packets must
identify one row by multiplier, edge, or stable hash before lane promotion.

The [priority-1 clause necessity matrix](evidence/p25_v2_priority1_clause_necessity_matrix_20260617.md)
now mutates the H0 and conductor-39 positive packets one required clause at a
time. All 18 damaged packets route to repair or reject rows, so the packet
intake has zero false-positive priority-1 source-stage closes.

The [conductor-39 row-binding overlay](evidence/p25_v2_conductor39_row_binding_overlay_20260617.md)
now tightens the conductor-39 packet path: the archived mixed `U_chi/W`
fixture remains a source-language positive shape, but it is not a
source-stage close until it binds to `m=1`, `m=2`, `m=4`, or `m=8` by edge or
stable row hash. Current row-bound conductor-39 source theorems remain zero.

The [Live Theorem Ask Packet](evidence/p25_v2_live_theorem_ask_packet_20260617.md)
is now the front-door action surface after those lookup rows. It reduces the
next expert, literature, or proof pass to three rows: first-pass scalar-fixed
row theorem, period-156 H0/Y507 value theorem, or exact-P/theta2 upstream
theorem. Broad source-family reread remains closed; a positive answer must
normalize to one scalar-fixed legal support-156 row or carry the accepted
exact-P packet before lane status changes.

The [current theorem kernel](evidence/p25_v2_current_theorem_kernel_20260617.md)
is the current compact review packet. It accepts only three source-stage
front doors: one scalar-fixed legal row, one row-labeled unique power
`e in {3,5,13,39,75,169,507}`, or one support-period-156 value with a legal
row bridge. `Q`/Yang remains support until selector and finite theorem debt
are paid, matched aggregate packets remain support until the exact
zero-lattice quotient theorem is supplied, exact-P/theta2 remains the heavy
upstream route via `75 -> 300 -> 12 -> 312 -> 156`, and reverse
unified-to-exact-P is rejected without extra selector structure. Current
source-stage closers and submission-ready rows remain zero.

The [source theorem acceptance automaton](evidence/p25_v2_source_theorem_acceptance_automaton_20260617.md)
now makes that kernel executable for incoming theorem-shaped replies. It has
three first-pass accept families, with seven explicit row-power accept rows
inside the unique-power family, one Q/Yang normalize-then-accept row, one
matched-quotient normalize-then-accept row, one exact-P/theta2 heavy route,
eleven repair rows, and two reject rows. Even-boundary distribution/norm
closure remains repair; aggregate relation data promotes only in the matched
packet form with exact `R^v`, exact `R^(v - (sum v)e_m)`, and
`gcd(sum v,p-1)=1`.

The [Drew kernel review packet](evidence/p25_v2_drew_kernel_review_packet_20260617.md)
is the current expert-facing version of that kernel. It is the packet to use
for Drew/source feedback because it explicitly separates row-power `R_m^75`
from exact-P 75 atoms, keeps the full unique-power set
`{3,5,13,39,75,169,507}`, and routes Q/Yang, matched aggregates, and
exact-P/theta2 answers as support or heavy upstream unless their exact finite
theorem clauses are present. Current positive expert answers and
submission-ready rows remain zero.

The [kernel consistency audit](evidence/p25_v2_kernel_consistency_audit_20260617.md)
now guards that reading path. It verifies that the theorem kernel, Drew
packet, source-stage spine, repair-debt matrix, extended unique-power intake,
live theorem ask packet, matched-quotient packet/source-feasibility screen,
period-156 supersession, source-theorem automaton, and canonical lane pages all
use the same seven-power set, inverse exponents, matched-quotient support
route, period-156 stop condition, and zero-closer status.

The [extended power-source coverage addendum](evidence/p25_v2_extended_power_source_coverage_addendum_20260617.md)
closes the follow-up local-source question from that expansion: `75/169/507`
row powers are intake-only exact row hooks, not a reason to reopen broad local
source search and not a shortcut from exact-P 75 atoms to `R_m^75`.
The [general unit-power intake](evidence/p25_v2_general_unit_power_intake_20260617.md)
now gives the full triage rule for future row-power snippets: exact finite
row-labeled `R_m^e` values root uniquely exactly when `gcd(e,p-1)=1`, while
powered boundary/divisor statements still need finite normalization. It also
adds a 23-branch warning: `R_m^23` and related 23-divisible powers have
nontrivial branch debt.

The [expert handoff supersession](evidence/p25_v2_expert_handoff_supersession_20260617.md)
now makes that reading order explicit: the older minimal expert ask and
first-pass expert intake packet are supporting provenance, while the current
expert/source-feedback path starts from the theorem kernel and Drew kernel
review packet. No canonical lane should call the older minimal ask the
preferred handoff now.

The [local source hook coverage audit](evidence/p25_v2_local_source_hook_coverage_audit_20260617.md)
now closes the local-corpus reread loop for those live rows. The local
Koo-Shin, KSY, Koo-Shin II, Sprang, Kubert-Lang/exact-P, Schertz/Scholl, and
Kato-Siegel surfaces are covered against the first-pass row theorem,
period-156 value route, conductor-39 Q/Yang support route,
row/quartic/power normalizers, and exact-P/theta2 heavy route. There are zero
uncovered local-source hook rows and zero local source-stage closers.

The [external source delta](evidence/p25_v2_external_source_delta_20260617.md)
adds the same boundary for the latest checked Kato-Siegel/Siegel-distribution
and Siegel-invariant sources. Beilinson-Kato distributions, Scholl's
Kato-Siegel notes, Kubert-Lang generator theorems, CM Siegel invariants, and
Siegel-Ramachandra generation remain support or repair rows until upgraded to
one accepted source-theorem automaton row. Current external source-stage
closers remain zero.

The [external finite-normalization scout](evidence/p25_v2_external_finite_normalization_scout_20260617.md)
narrows that near miss further. Kato-Siegel supplies a canonical modular-unit
normalization at the divisor/isogeny level, and nearby Siegel-unit sources
give generator, class-field, regulator, and modular-curve model machinery, but
none supplies the p25 support-156 row selector plus finite `F_p`
value/additive/telescoping payload. Current finite-normalization closers
remain zero.

The [orphan evidence supersession audit](evidence/p25_v2_orphan_evidence_supersession_audit_20260617.md)
accounts for four older v2 notes that remain outside the cockpit marker list:
the exact-P theorem-interface contract, H0/conductor-39 frontier pass,
Koo-Shin II first-pass scan, and KSY source ingest scan. Their live content is
already absorbed downstream; no source-stage closer, exact-P producer, or broad
reread obligation is hidden there.

The [Kato-Siegel divisor-source scout](evidence/p25_v2_kato_siegel_divisor_scout_20260617.md)
now sharpens the closest external theorem-shaped near miss. Kato-Siegel
functions supply canonical divisor/norm-compatible modular-unit machinery, but
the inspected sources do not supply the p25 row-selected finite `F_p`
value/additive/telescoping payload. Future Kato-Siegel citations are repair
rows unless they specialize to one legal support-156 row and fix the scalar.

The [Fp-star branch factorization](evidence/p25_v2_fpstar_branch_factorization_20260617.md)
now pins the finite-field branch arithmetic behind value and power claims:
`p - 1 = 2^2 * 11 * 23 * 9881422924901185770751`. Exact `3`, `5`, `13`,
`39`, `75`, `169`, and `507` power values are uniquely rootable,
`mu_11`/`mu_44` ambiguity is real, and a primitive order-39 scalar is absent
from `F_p` even though the 39th-power map is bijective.

The [extended unique-power intake](evidence/p25_v2_extended_unique_power_intake_20260617.md)
adds one narrow consequence: exact `R_m^75`, `R_m^169`, or `R_m^507` source
theorems would also uniquely recover `R_m`. These are not new broad search
lanes; they are accepted only if a theorem arrives already carrying the exact
finite row-power value, row label, and boundary/period bridge.

The [Koo-Shin priority-1 top-row falsifier](evidence/p25_v2_koo_shin_priority1_toprow_falsifier_20260617.md)
closes the first lookup row against the local 2010 paper itself. The raw
extract contains Theorem 5.2, Lemma 6.1, Theorem 6.2, Corollary 7.3, and the
Section 9 ray-class generator surfaces, but no `Norm_156`, `Y_507`,
Hilbert-90, period-156, support-156, basepoint/telescoping, or finite
scalar-fixing row theorem. Koo-Shin remains source legality/context, not the
priority-1 closer.

The [mixed signed-column fingerprint](evidence/p25_v2_mixed_signed_column_fingerprint_20260616.md)
now records the source-side shape those rows must preserve: each legal
conductor-39 potential is a six-column signed matching in the `C_3 x C_13`
grid, has zero proper pushforwards, and shares the same Hilbert-90 boundary
`W`. Formal one-coset boundary controls are rejected because they match the
boundary while losing this mixed-column fingerprint.

The [mod-13 coset rectangle](evidence/p25_v2_mod13_coset_rectangle_20260616.md)
compresses that fingerprint: with `H=<3>=(1,3,9)`, each legal row chooses one
nonsquare order-3 coset (`2H` or `7H`) as row-1 plus columns and one square
order-3 coset (`H` or `4H`) as row-1 minus columns. Pure quadratic-character,
same-parity-coset, or non-coset-triple claims are too broad or wrong.

The [C4 character spectrum](evidence/p25_v2_c4_character_spectrum_20260616.md)
now makes the character-language obstruction explicit. Each legal edge has
DFT spectrum `(0, phase, -2, conjugate phase)` with nonzero order-4 `C4`
components. The pure quadratic odd-minus-even aggregate has spectrum
`(0,0,-4,0)`, so quadratic-character-only source theorems are repair rows
unless they also supply the order-4 phase selecting one edge.

The [row-sign x C4 tensor spectrum](evidence/p25_v2_row_sign_c4_tensor_spectrum_20260616.md)
combines that with the mod-3 row sign. Each legal conductor-39 source row has
zero proper pushforwards, zero row-symmetric component, and lives in the
row-antisymmetric tensor component with nonzero order-4 `C4` phases. Projection
only, row-symmetric, row-sign-only, C4-only, or quadratic-only source claims
are therefore repair/reject unless they recover the full tensor edge and a
finite value/divisor theorem.

The [Frobenius tensor eigenboundary](evidence/p25_v2_frobenius_tensor_eigenboundary_20260616.md)
now explains why boundary-only data cannot recover that edge selector. In the
row-antisymmetric quotient-`C4` spectrum, the order-4 selector components are
Frobenius-invariant and killed by `(1 - Frob_p)`, while the common quadratic
component is Frobenius-anti-invariant and maps to the shared boundary. Thus
all four legal edges have the same boundary spectrum, and boundary-only,
norm-only, or quadratic-character theorems are repair rows unless they supply
the source-side order-4 selector and finite theorem.

The [quartic selector payload](evidence/p25_v2_quartic_selector_payload_20260616.md)
now states the minimal positive character-language selector. The exact
row-antisymmetric `C4_1` phase distinguishes the four legal edges:
`2+2i`, `-2+2i`, `-2-2i`, and `2-2i`. One sign of that phase leaves a two-edge
ambiguity, and magnitude/quadratic data leaves all four edges. A promising
character-language theorem must therefore give the `W` boundary, the exact
quartic phase in the mixed tensor setting, and the scalar-fixed finite theorem.

The [quartic reciprocal orientation](evidence/p25_v2_quartic_reciprocal_orientation_20260616.md)
screen adds the orientation warning to that selector: reciprocal rows negate
both the boundary and the `C4_1` phase, and the negated phase is also the phase
of the opposite oriented edge. Thus an exact quartic phase still needs oriented
row data or the correct boundary sign; reciprocal phase plus positive boundary
is rejected.

The quartic selector candidate sweep consolidates those screens for future
source intake: prior quartic, projector, and Q-split artifacts are not hidden
theorem hits. They remain repair/support rows unless upgraded to a direct
one-edge theorem, a selected-root projector theorem, or Q diagonal plus pure
quartic split with an oriented root/sign and the same finite theorem data.

The [quotient-H90 idempotent mechanism](evidence/p25_v2_quotient_h90_idempotent_mechanism_20260616.md)
explains why those four rows have the same boundary: Frobenius shifts the
`(Z/13Z)^*/<3> ~= C4` coset index by `2` and flips the `C_3` row. The first-pass
source object is therefore a sparse odd/even quotient-`C4` idempotent
difference `V_{o,e}`, not a full quadratic-character row.

The [minimal H90 preimage classifier](evidence/p25_v2_minimal_h90_preimage_classifier_20260616.md)
now closes the sparse-selector question: `W` has `2^4 = 16` minimal integral
support-12 preimages, and exactly four have both proper-axis pushforwards zero.
The other twelve are boundary controls, not source closers.

The [H90 support lower bound](evidence/p25_v2_h90_support_lower_bound_20260616.md)
promotes the reason for that rigidity: on each of the four length-six
Frobenius orbits, the local boundary pattern is `(-6,6,-6,6,-6,6)`, so a
constant shift can kill at most three positions. Thus every H90 preimage of
the current `W` has support at least 12, with exactly the 16 support-12
minimizers already classified. The source-snippet intake and expert-response
rubric now use this as a hard falsifier: support-below-12 H90 selector claims
are rejected, and support-12 claims must pass the four-row mixed-source screen.

The [Koo-Shin distribution noncloser](evidence/p25_v2_koo_shin_distribution_noncloser_20260616.md)
now records the bounded post-selector read of Koo-Shin 2010: Theorem 5.2,
Lemma 6.1, and Theorem 6.2 are distribution/root-descent/legality helpers, but
the four legal quotient-`C4` rows are nonconstant two-positive/two-negative
rectangle rows, so those clauses do not emit the missing p25 value/divisor
theorem.

The [Theorem 5.2 constant-span obstruction](evidence/p25_v2_theorem52_constant_span_obstruction_20260616.md)
now kills the natural repair "multiply powers of legal rows until Koo-Shin 5.2
applies": the four legal quotient-`C4` patterns are `+/-A` and `+/-B`, and
`span_Z{A,B}` meets the constant-exponent line only at zero. A Theorem 5.2
route would therefore need an independent finite theorem, not a nontrivial
combination of the current legal rows.

The [Koo-Shin access-blocker closure](evidence/p25_v2_koo_shin_access_blocker_closure_20260616.md)
now marks the old external bridge action as superseded. The earlier
`candidate_needs_pdf_or_ocr_before_theorem_use` row is no longer a live
retrieval task; the v2 scans used the paper/extract and found source legality,
not an exact-P bridge, Q-route hook, scalar-fixed finite theorem, or
packetizable p25 payload.

The [rectangle diagonal aggregate](evidence/p25_v2_rectangle_diagonal_aggregate_20260616.md)
now pins another tempting broadening: `m1*m4 = m2*m8` gives the broad
quadratic aggregate with boundary `2W`, and the all-four product is its square
with boundary `4W`. These are useful context, but they are repair rows unless
they also select or factor to one sparse rectangle edge with the original `W`
boundary.

The [row quotient invariant bridge](evidence/p25_v2_row_quotient_invariant_bridge_20260616.md)
records the complementary factorization data: all six nontrivial quotients of
legal rows have zero Hilbert-90 boundary, and a diagonal aggregate plus the
matching quotient recovers twice one legal row. This is useful structure, but
still a repair row unless the source supplies halving/root/orientation data or
a direct one-row theorem.

The [row square root ambiguity](evidence/p25_v2_row_square_root_ambiguity_20260616.md)
now makes that repair precise: a row-square value has roots `R` and `-R`, and
the constant sign has zero divisor and zero Hilbert-90 boundary. A square or
doubled-`2W` theorem therefore does not select the oriented legal row unless it
also supplies explicit root/sign/orientation data.

The [coefficient-6 root normalization](evidence/p25_v2_coefficient6_root_normalization_20260616.md)
now records the p25-specific power-map arithmetic for the current rows:
all legal products have coefficients `+/-6`, `gcd(3,p-1)=1`, and
`gcd(6,p-1)=2`. Exact lower-coefficient root theorems can be powered back to
the current coefficient-6 row, but extracting a square or sixth root from
coefficient-6 data remains a repair row without oriented root/sign data.

The [power/scalar ambiguity inventory](evidence/p25_v2_power_scalar_ambiguity_inventory_20260616.md)
extends that screen to value powers and root-of-unity scalars: exact cube,
fifth, thirteenth, or thirty-ninth powers are uniquely invertible in `F_p^*`,
while square, fourth, eleventh, twenty-second, forty-fourth, 156th, and 780th
powers need branch/orientation/scalar data. `mu_11` and `mu_44` scalar
ambiguities are real in `F_p`; `mu_39` is not an `F_p` scalar even though the
39th-power map is bijective.

The [power-normalized theorem intake](evidence/p25_v2_power_normalized_theorem_intake_20260616.md)
promotes the source-theorem consequence of that arithmetic: a challenge-legal
source theorem for exact `R_m^e`, `e in {3,5,13,39,75,169,507}`, attached to
one legal row and the accepted boundary/period bridge, can be uniquely rooted
back to the ordinary row-value intake. The `75/169/507` hooks are accepted
only if they arrive already exact and row-labeled, not as broad search targets.
Exact square, fourth, eleventh, twenty-second, forty-fourth, 156th, or 780th
powers remain repair without extra branch/orientation/scalar data.

The [power output-kind router](evidence/p25_v2_power_output_kind_router_20260616.md)
now prevents over-crediting that fact: an exact finite value for `R^e` can be
rooted when the power map is bijective. It now explicitly routes exact
`R^e` value theorems for `e in {3,5,13,39,75,169,507}` with inverse exponents
modulo `p-1`, but a divisor/H90 theorem or boundary statement for a powered
row still needs finite value/additive normalization before root recovery.

The [primitive character power recheck](evidence/p25_v2_primitive_character_power_recheck_20260617.md)
kills the nearest old shortcut: `V_bal = U_chi^3` and `W = U_chi^6` are
source-unit exponent relations, not exact finite `F_p` row-power value
theorems. A future `U_chi`/`V_bal` answer remains live only if it gives an
exact finite value theorem, one legal row or row-labeled theorem, and the
accepted `Norm_156(Y_507)` boundary/period bridge.

The [power candidate sweep](evidence/p25_v2_power_candidate_sweep_20260617.md)
then audits the wider prior artifact forest under the same rule. Router rows
are intake templates, coefficient-root rows are conditional theorem shapes,
primitive-character powers are support identities, and Lane B `D^3=Y` is a
quotient/kernel monodromy relation. No prior artifact is newly promoted to a
source-stage closer; the future power ask remains exact finite `F_p` value
data for one legal row plus the accepted boundary/period bridge.

The [power/projector extraction boundary](evidence/p25_v2_power_projector_extraction_boundary_20260616.md)
now aligns those power routes with the Q-square rule. Exact unique-power
values, or ambiguous-power values with a selected root, route back through
source-snippet intake. Exact unselected square, fourth/projector, eleventh, or
156th-power values are bounded row-value payloads only; their roots are not
`vpp.py` candidates without orientation/branch/scalar data and a DANGER3
same-j/X_1(16)/halving or direct `(A,x0)` extraction map.

The [constant normalization ambiguity](evidence/p25_v2_constant_normalization_ambiguity_20260616.md)
generalizes that warning from signs to arbitrary `F_p^*` scalars: divisor plus
Hilbert-90 boundary data cannot distinguish `R` from `cR` for `c in F_p^*`.
A source answer that is only up to scalar is now a repair row unless it also
supplies additive/value normalization, period-156 branch/root/telescoping
context, or finite framing that fixes the scalar.

The [norm-only descent ambiguity](evidence/p25_v2_norm_only_descent_ambiguity_20260616.md)
now separates dense boundary/norm data from the selected row: `Norm_156(Y_507)`
has support `312`, while each legal Hilbert-90 preimage/product row has support
`156`. A norm identity or value is repair unless it descends to one legal
support-156 row and then supplies the finite theorem for that row; formal
one-coset descents are rejected because they share the boundary while failing
the mixed-axis tests.

The [post-theorem extraction router](evidence/p25_v2_post_theorem_extraction_router_20260616.md)
classifies any future theorem hit through the DANGER3 ladder:
source theorem, finite/non-CM framing, same-`j` `X_1(8112)`, practical
`X_1(16)`, halving/direct `x0`, and official `vpp.py`.

The [extraction payload contract](evidence/p25_v2_extraction_payload_contract_20260616.md)
now makes that ladder constructive: independent `P16` and `Q507` data are
rejected without same-j gluing; matching `j` alone is still only a repair row
without explicit same-curve torsion data or direct `A,xP16`; a practical
`X_1(16)` payload is either `y` plus model root or direct `A,xP16`; the
optional d-gate surface is not required; and branch words without concrete
values are not certificates.

The [extraction minimal hook](evidence/p25_v2_extraction_minimal_hook_20260616.md)
is now the shortest post-theorem acceptance boundary. After a source-stage
theorem or exact-P hook, a certificate still needs DANGER3 finite-identity or
non-CM framing, same-`j` `X_1(8112)` data or an order-8112 generator, a
practical `X_1(16)` payload (`A,xP16` or `y` plus model root), 38 halving links
or direct `x0`, and official `vpp.py` verification. Current extraction-ready
and submission-ready row counts remain zero.

The [candidate packet intake reorg](evidence/p25_v2_candidate_packet_intake_reorg_20260616.md)
restores the executable front door for future theorem/expert packets after the
wiki reorganization. Exact H0 product fixtures now resolve from
`archive/fixtures/h0_product_fixtures`, the legacy harness-side duplicate is
absent, and the packet gate enforces source theorem, DANGER3 framing, same-j
bridge, `X_1(16)` payload, halving/direct `x0`, and official `vpp.py` in that
order. Current real submission-ready packet rows remain zero.

The [source-snippet intake](evidence/p25_v2_source_snippet_intake_20260616.md)
is now the default filter for paper snippets or expert replies after row-orbit
normalization, conductor-39 selector classification, source-graph edge
normalization, and edge-lattice coefficient checks. It checks legal payload
hashes, source-theorem provenance, Koo-Shin Theorem 5.2 constant-span repairs,
period-156 context, scalar normalization, and then routes any real hit into the
post-theorem extraction ladder. It now also distinguishes dense norm-only
answers from legal Hilbert-90 descents to one support-156 row, degree-6
value/orbit claims from explicit `F_p` descents to a selected legal row,
conductor-39 source identification from Yang lift/H90 boundary/finite theorem
content, lower-coefficient exact-root theorems from unoriented root-extraction
shortcuts, uniquely invertible power values from real root-of-unity scalar
ambiguity, and non-unit `W`-boundary edge combinations from direct one-edge
theorems. The unique power-value branch now explicitly covers exact `R^e`
values for `e in {3,5,13,39,75,169,507}`. It now also routes quartic character
snippets: exact `C4_1` plus a finite theorem is a source-stage shape,
selector-only or coarse phase data is repair, reciprocal phase with
missing/wrong boundary sign is repair or reject, and same-parity quartic phase
is rejected. It now also consumes compact conductor-39 `Q`-route snippets:
`Q` value theorems with period-156 context and finite `Q^3` H90 theorems are
support routes; `Q` source-only, `Q^6` boundary-only, primitive-power-only, and
pure degree-6 norm claims are repair or reject.

The [source-family gap matrix](evidence/p25_v2_source_family_gap_matrix_20260616.md)
is now the compact literature-search boundary. It records seven inspected
source families with useful vocabulary, only Koo-Shin 2010 currently matching
the one-edge source-object shape, and zero inspected scalar-fixed finite
theorems, period-156 value theorems, exact-P upstream theorems, first-pass
closers, or submission-ready rows.

The [minimal expert ask](evidence/p25_v2_minimal_expert_ask_20260616.md)
is now supporting predecessor context for the Drew kernel review packet. It
still records the short yes/no theorem question for a scalar-fixed finite
divisor/additive identity or support-period-156 value theorem for one exact
oriented quotient-`C4` edge `R_m`, with `Norm_156(Y_507)` boundary, and if
phrased in character language, exact row-antisymmetric `C4_1` phase data with
oriented row/boundary-sign convention.
It now also includes the compact conductor-39 `Q` support-route question:
finite `Q` value theorem with period-156 context, or finite `Q^3` Hilbert-90
preimage theorem, with source-only and boundary-only `Q` answers routed as
near misses. The Q-route selector-debt screen keeps this as support language,
not a source-stage close, until the order-4 edge selector debt is paid.
It classifies source-legality, boundary-only, unspecified-scalar, period-780,
degree-6, non-unit-edge, aggregate, coarse-quartic, reciprocal-phase,
exact-P-vocabulary-only, finite-payload, projector-without-root,
two-edge-pair-without-oriented-root, branchless exact-P orientation,
theta2-without-period-156-context, wrong exact-P packet, and generic
CM/class-field answers as repair/reject until they supply the missing theorem.
The exact-P escape hatch is now routed through the exact-P minimal hook and
orientation branch router before it can count as an upstream theorem route.

The [additive-normalizer source scan](evidence/p25_v2_additive_normalizer_source_scan_20260616.md)
applies the additive contract to the local Koo-Shin/KSY extracts. It found
helper vocabulary in all three local sources, but no basepoint, telescoping,
period-156/H90/Y507, or scalar-fixing finite additive/value theorem that would
promote the first-pass divisor/additive row.

The [DANGER3 finite-identity framing contract](evidence/p25_v2_danger3_finite_identity_framing_contract_20260616.md)
now pins the first downstream boundary after a source theorem: generic CM,
ray-class, class-field, or unit-generation statements are not DANGER3 framing
unless they specialize to an exact p25 finite identity and supply non-CM
finite-field framing or an external policy yes.

The [current expert response rubric](evidence/p25_v2_current_expert_response_rubric_20260616.md)
is now the one-page classifier for Drew/source answers. It combines the
theorem review packet, row normalization, minimal H90 selector classifier,
source graph normal form, orbit-tuple router, K22 automorphism quotient
falsifier, edge-lattice classifier, mod-13 rectangle, C4 character spectrum,
row-sign x C4 tensor spectrum,
quartic selector payload, quotient-H90 mechanism, Koo-Shin distribution
noncloser, Theorem 5.2
constant-span obstruction, period-156 value contract, source intake,
scalar-normalization ambiguity, norm-only/descent ambiguity, degree-6 descent
ambiguity, Yang lift/descent boundary contract, DANGER3 finite-identity
framing, conductor-39 norm-one quotient routing, coefficient-6 root
normalization, power/scalar ambiguity inventory, power output-kind router,
reverse exact-P information loss, and extraction contract into accepted,
repair, reject, extraction, and submission rows.

The [snippet/expert intake sync](evidence/p25_v2_snippet_expert_intake_sync_20260617.md)
keeps those older broad routers aligned with the live automaton. Both the
source-snippet intake and expert rubric now explicitly defer theorem
classification to the automaton for the full unique-power set
`e in {3,5,13,39,75,169,507}` and the
`repair_even_boundary_distribution_closure` row. Current source-stage closers
and submission-ready rows remain zero.

## Secondary Heavy Route

- [Exact P](lanes/exact-p.md) remains the headline prize.
- It is alive as the stronger upstream producer route into the same unified
  H0/conductor-39 finite target. No inspected source emits the exact 75-atom
  product, its orientation, and the arithmetic theorem needed to activate the
  `75 -> 300 -> 12 -> 312 -> 156` bridge.
- The latest exact-P support passes give positive finite artifacts: the
  accepted theta2 footprint forces all 75 normalized-y atom weights equally,
  and the theorem interface has been compressed to a rigid
  `C=(47,28), D=(22,3), K=(57,0), orientation` packet, or an equivalent
  period-156 theta2 divisor/additive payload. The exact-P-to-unified pass
  verifies that such a theorem would feed the unified support-156 target, but
  the implication is one-way.
- The [exact-P finite-geometry rigidity](evidence/p25_v2_exactp_finite_geometry_rigidity_20260616.md)
  is now cockpit-tracked: the 75 atoms have disjoint four-cell support, force
  full rank, force equal weights for theta2/theta2-inverse, and reject
  missing-atom, nonuniform-weight, and generic ray-class-generator escapes.
- The [exact-P minimal hook](evidence/p25_v2_exactp_minimal_hook_20260616.md)
  is the compact heavy-route ask: a source theorem must emit the
  `C,D,K,orientation` packet, the exact equal-weight 75-atom theorem, an
  accepted theta2 payload, or an explicit reverse reconstruction theorem.
  Normalized-y vocabulary, raw Kubert-Lang balance, generic ray-class
  generation, and unified-target theorems without exact-P selector data are
  repair or reject rows.
- The [exact-P candidate sweep](evidence/p25_v2_exactp_candidate_sweep_20260617.md)
  closes the "maybe already enough" exact-P family: KSY vocabulary, KL
  screens, finite fixtures, theta2 support, Sprang support, period-156 value
  bridges, and unified-target theorems remain scaffolding unless they emit one
  of the accepted exact-P theorem hooks.
- The [exact-P/theta2 lookup-row status](evidence/p25_v2_exactp_theta2_lookup_row_status_20260617.md)
  compresses the heavy upstream lookup row. Exact-P remains live only through
  compact `C,D,K,orientation`, exact equal-weight 75 atoms, exact theta2/
  theta2-inverse payload with period-156 bridge, theorem-legal KL primitive
  word or mixed selector, Sprang sparse specialization, or explicit reverse
  reconstruction. Generic normalized-y, KL, Sprang, theta, distribution, or
  unified-target language remains support/repair.
- The [exact-P 75-anchor bridge filter](evidence/p25_v2_exactp_75_anchor_bridge_filter_20260617.md)
  prevents a new false shortcut after the common-scalar pass. Exact
  row-labeled `R_m^75` would be a first-pass row anchor because
  `gcd(75,p-1)=1`, but the exact-P equal-weight 75 atoms live in the
  normalized-y/theta2 basis and are a heavy upstream theorem only when a source
  supplies orientation and the `75 -> 300 -> 12 -> 312 -> 156` bridge.
- The [exact-P spine payload separation](evidence/p25_v2_exactp_spine_payload_separation_20260617.md)
  records the finite support ladder behind that bridge:
  `75 -> 300 -> 12 -> 312 -> 156`, with final H90 lift `+78/-78`. This keeps
  three claims separate: fixed exact-P atoms are not 75 tries, the 156-support
  H90 product is not the plain 75-atom product, and unified support-156 does
  not recover exact-P without selector data.
- The [KL primitive-word source split](evidence/p25_v2_kl_primitive_word_source_split_20260617.md)
  sharpens the KL hook to either the oriented six-term primitive word or the
  three-term Hilbert-90 source chain with unique boundary step, still requiring
  K-trace/theta2 period-156 context and an arithmetic source theorem.
- The [KL source-split local scan](evidence/p25_v2_kl_source_split_local_scan_20260617.md)
  checks that hook against raw local KSY/Sprang/Koo-Shin extracts. The texts
  supply helper vocabulary, but no exact primitive-word, H90-chain,
  boundary-step, or K-trace/theta2 source theorem.
- The [KL cyclotomic norm route audit](evidence/p25_v2_kl_cyclotomic_norm_route_audit_20260617.md)
  classifies modular units inside cyclotomic units, Robert-unit module
  relations, and regulator-level formulas as support/normalizer context. They
  do not select one p25 support-156 row or recover exact-P without
  `C,D,K,orientation`, equal-weight atoms, theta2 payload, or explicit reverse
  reconstruction.
- The [reverse exact-P information-loss checkpoint](evidence/p25_v2_reverse_exactp_information_loss_20260616.md)
  makes that one-way boundary operational: a unified support-156 theorem routes
  to post-theorem extraction, not to exact-P recovery, unless it also supplies
  the missing `C,D,K,orientation`, equal-weight 75-atom selector, period-156
  theta2 payload, or explicit reverse theorem.
- The [exact-P closure-template replay boundary](evidence/p25_v2_exactp_closure_template_replay_boundary_20260616.md)
  keeps the old closure-template gate as archive provenance, not the default
  probe during active production. Normal source/expert intake should use the
  v2 exact-P theorem interface, minimal hook, orientation router, and theta2
  period-156 support contract unless a theorem-shaped snippet justifies a
  heavyweight replay.
- The [McCarthy endpoint stability router](evidence/p25_v2_mccarthy_endpoint_stability_router_20260617.md)
  promotes the archived square-axis microscope to a compact support verdict:
  the sparse endpoint `e_138` on `C_507` and
  `1 + (zeta_39^5 - 1)e_138` round-trip through normalized raw-Y closure, but
  the raw `R(138)^2029` projection is not auxiliary-prime invariant and 236
  simple target-factor normalizations find no all-prime repair. Treat future
  McCarthy/Barnes leads as endpoint-theorem tests, not as an active
  first-pass front.

## Support Lanes

- [Twisted H90](lanes/twisted-h90.md): background support surface, not an
  independent first-pass front; continue only if a finite value/divisor theorem
  arrives with period-156 context.
- [Curved Corner](lanes/curved-corner.md): background support surface, not an
  independent first-pass front; continue only if an exact unit-triangle
  curved-corner theorem arrives with the same context.
- The p24-style CM/Lang transfer is effectively dead; the remaining
  fixed-frequency/Jacobi and low-moment/W-axis ideas are now support
  microscopes inside the [transfer matrix](concepts/transfer-matrix.md), not
  current front-door claims.

The [support lane router](evidence/p25_v2_support_lane_router_20260616.md)
makes that demotion explicit: Twisted-H90 and curved-corner snippets must
enter through the period-156 value source hook and source-snippet intake, while
exact-P snippets enter through the exact-P minimal hook. The support lanes
currently add zero independent front-door closers.
The [support microscope router](evidence/p25_v2_support_microscope_router_20260617.md)
extends that rule to generic Lane-B/Lane-C probes and the McCarthy square-axis
endpoint. Generic fixed-frequency/Jacobi or low-moment/W-axis probes must be
tied to H0, conductor 39, exact-P, or another named theorem idea; the McCarthy
endpoint continues only as sparse `e_138` theorem intake or an
auxiliary-prime-invariant cancellation test. It adds zero source closers and
zero first-pass front doors.
The [support lane status demotion](evidence/p25_v2_support_lane_status_demotion_20260616.md)
now aligns their wiki status with that rule: both lanes are background
support surfaces, not independent first-pass fronts.

The [end-to-end answer router](evidence/p25_v2_end_to_end_answer_router_20260616.md)
now stitches the intake and extraction boundaries into one classifier. Future
answers stop at the first failed stage: source intake, DANGER3 finite-identity
framing, same-j `X_1(8112)`, practical `X_1(16)`, halving/direct `x0`, or
official `vpp.py`. Only the final stage is submission-ready.

## Decisive Blockers To Submission

- No source-stage closing theorem has been found for the unified first-pass
  target or the stronger exact-P upstream route.
- The first-pass combinatorial layers are saturated: source legality,
  Hilbert-90 boundary, sparse Yang lift, and 78-over-78 product normal form are
  artifacts, not closers.
- Seed-ratio or proper-suborbit conductor-39 shortcuts are falsified unless
  they include an explicit legality repair.
- The preferred theorem ask is now the value/divisor interface row: finite
  divisor/additive identity for one legal support-156 product with
  `Norm_156(Y_507)` boundary.
- Value-side claims must pass the period-156 branch contract; ambient-period
  `780` value statements, `mu_11` quotient or 11th-power statements, degree-6
  values without descent/context, direct order-39-root shortcuts, and
  `sqrt(-39)` scalar shortcuts do not close source stage.
- Current source-family routing has zero closers in hand; broad Koo-Shin, KSY,
  Sprang, Kubert-Lang, or Schertz/Scholl reading is a stop sign unless a source
  snippet is already shaped like the accepted theorem rows.
- Value-side source work should now pass through the period-156 value source
  hook before promotion; Schertz/Shin/Scholl field-generation or ambient-value
  language is still not a finite p25 value theorem.
- The expert-review packet has zero source theorems and zero submission-ready
  rows; it is a precise ask, not evidence that the theorem exists.
- The group-ring payload removes ambiguity in the finite target, but it is
  still not a source-stage close without the value/divisor theorem.
- Row presentations should be normalized before intake; demanding all four
  rows overstates the ask, while accepting outside-doubling-orbit multipliers
  understates it.
- Outside-doubling units are reciprocal presentations, not oriented legal rows.
  A reciprocal theorem must carry the `-Norm_156(Y_507)` boundary sign or be
  rewritten as an oriented legal row before intake; reciprocal-plus-boundary
  claims are falsified.
- Conductor-39 source answers must preserve the signed-column `C_3 x C_13`
  fingerprint or explicitly transform into a normalized row; prime projection,
  pullback, additive-separated, and formal one-coset boundary answers are
  falsified as first-pass closers.
- Column-level conductor-39 answers must land on one mod-13 rectangle edge:
  one nonsquare order-3 coset against one square order-3 coset. A pure
  quadratic-character mod-13 statement is too broad.
- Boundary explanations should identify the sparse quotient-`C4` idempotent
  difference mechanism. Same-parity edges have zero boundary and the full
  quadratic-character row has boundary `2W`, so neither is the active source
  object.
- Character/projector decompositions reconstruct `4*edge`, not `edge`.
  Since `gcd(4,p-1)=4`, projector values without selected fourth-root or scalar
  data are repair rows, not source-stage closers.
- Hilbert-90 boundary data erases the edge-selecting order-4 quotient-`C4`
  phases: `(1 - Frob_p)` kills the Frobenius-invariant selector components and
  sees only the common quadratic component. Boundary-only, norm-only, and
  quadratic-character answers therefore need source-side selector data before
  promotion.
- The minimal positive character selector is the exact row-antisymmetric
  `C4_1` phase. One phase sign gives only a pair, and phase magnitude gives all
  four rows; neither is source-stage complete without the missing sign and
  finite theorem.
- Two-edge pair claims have boundary `2W`, and pair-difference claims have
  boundary zero. Pair plus difference reaches `2*edge`, so it still needs an
  oriented square root before source-stage promotion.
- Broad aggregate answers should not be promoted as source closes: diagonal
  products have boundary `2W`, and the all-four product has boundary `4W`,
  unless the answer also selects or factors to one sparse `W`-boundary edge.
- Row-quotient answers are boundary-zero relations between legal rows, not
  one-row source theorems. A diagonal aggregate plus a matching quotient reaches
  a row square; it still needs halving/root/orientation data before promotion.
- Row-square answers are not one-row source theorems unless they name the
  oriented root: `R` and `-R` have the same square and the same divisor/H90
  boundary, so doubled-boundary data alone is a repair row.
- Divisor/H90 answers that determine the value only up to an unspecified
  `F_p^*` scalar are not one-row source theorems. They need additive/value
  normalization, period-156 branch/root/telescoping context, or finite framing
  that fixes the scalar before source-snippet intake can promote them.
- The [additive normalization contract](evidence/p25_v2_additive_normalization_contract_20260616.md)
  makes the preferred divisor/additive row stricter: "additive" must mean a
  finite additive, value, basepoint, branch/root, or telescoping datum that
  fixes the `F_p^*` scalar. Divisor classes, boundary statements, dense
  additive relations, and up-to-scalar values remain repair rows.
- The [constructive value payload contract](evidence/p25_v2_constructive_value_payload_contract_20260616.md)
  now adds the packet-intake boundary. A source hit must also give
  deterministic finite data: an exact `F_p` row value, evaluable finite
  additive/telescoping formula, period-156 branch payload, or exact product
  packet paired with the source theorem. Existence/generation language,
  scalar-fixed but unevaluable theorems, local payloads without source, and
  treating a row value as direct `vpp.py` input remain repair or reject rows.
- The [constructive payload source scan](evidence/p25_v2_constructive_payload_source_scan_20260616.md)
  applies that boundary to the local Koo-Shin, KSY, and Sprang texts. All five
  source rows have helper vocabulary, but none names a p25 constructive payload
  hook such as `Norm_156`, `Y_507`, period-156 data, `C,D,K`, exact theta2,
  exact product packet, `A,x0`, or `vpp.py`. Current packetizable source
  payloads remain zero.
- The [value payload reality ledger](evidence/p25_v2_value_payload_reality_ledger_20260616.md)
  keeps the current evidence buckets separate. Fixed atoms, row hashes,
  fixtures, and packet files are verifier targets, not source theorems; a
  source theorem would still need DANGER3 framing, extraction, and official
  `vpp.py` before becoming a submission. Current source theorems and
  submission-ready rows remain zero.
- Lower-coefficient exact-root theorems must be powered back to the current
  coefficient-6 row before intake. Trying to infer a square or sixth root from
  coefficient-6 data without oriented root/sign data remains repair.
- Exact power-value claims split by the `F_p^*` power-map kernel: cube, fifth,
  thirteenth, and thirty-ninth powers are uniquely invertible; square, fourth,
  eleventh, twenty-second, forty-fourth, 156th, and 780th powers still need
  branch/orientation/scalar data. `mu_11`/`mu_44` ambiguity is real, while
  `mu_39` as an `F_p` scalar is rejected.
- Powered divisor/additive or H90-boundary claims are not the same as exact
  finite power values. They need finite value/additive normalization before a
  power-map inverse can promote them; boundary `eW` alone is not the current
  `W` boundary.
- Dense norm-only answers are not one-row source theorems. A theorem for
  `Norm_156(Y_507)` or its value still needs legal support-156 Hilbert-90
  descent selecting one row, and then the finite theorem for that selected row.
- Formal one-coset descents remain rejected even when their boundary equals
  `Norm_156(Y_507)`, because they fail the mixed-axis pushforward tests.
- Minimal Hilbert-90 selector freedom is exhausted: among all 16 support-12
  preimages of `W`, only the four current rows preserve both proper axes. The
  next progress must be an arithmetic value/divisor theorem, not another sparse
  selector.
- Koo-Shin 2010's distribution clauses can police a future source theorem but
  do not close the current target: Theorem 5.2 constant-product rigidity does
  not match the four nonconstant quotient-`C4` legal rows, while Lemma 6.1 and
  Theorem 6.2 provide distribution and legality rather than finite p25 values.
- Generic distribution/norm relation closure is now parity-blocked as a direct
  row source: aggregate rows have even `W`-boundary scale and quotient rows have
  boundary zero, so one boundary-`W` edge still needs direct source theorem,
  root/selector/scalar normalization, or equivalent repair data.
- No nontrivial product of the four legal quotient-`C4` rows repairs this:
  their integer span has no nonzero constant-exponent vector, so Koo-Shin 5.2
  cannot be activated by multiplying powers of the current legal rows.
- A future source-stage theorem is not submission-ready until it advances
  through the post-theorem extraction router to a verified `(p,A,x0)` triple.
- Generic CM/ray-class/class-field generation is not DANGER3 framing. A future
  source theorem must specialize to an exact p25 finite identity and either
  supply non-CM finite-field framing or receive an external policy yes before
  we treat it as extraction-unblocked.
- A future extraction payload must satisfy the extraction contract: same-j
  bridge, practical `A,xP16` or `y` plus model root, halving/x-chain/direct
  `x0`, and official `vpp.py`.
- The extraction minimal hook is now the compact checklist after any theorem
  hit or exact-P hook; no live row satisfies it yet.
- Matching `j` or an isomorphism class alone is not a same-j bridge; it still
  needs a same-curve `P16/Q507` pair, an order-8112 generator, or direct
  `A,xP16`.
- Do not promote future source snippets without first classifying them through
  the source-snippet intake.
- Do not promote future expert answers without first classifying them through
  the current expert response rubric.
- No live source object has yet been extracted into the same-`j`
  `X_1(8112)` / `X_1(16)` / halving payload that DANGER3 actually consumes.
- The submission/extraction ladder is now defined, but no live payload has
  entered it: no source theorem, no same-`j` bridge values, no `A,xP16`,
  no halving chain/direct `x0`, and no verified p25 triple.
- No `vpp.py`-verified p25 triple exists yet.

## Canonical Reading Path

1. Start here.
2. Choose [Practical search](lanes/practical-search.md) for operations or
   [H0](lanes/h0.md) / [Conductor 39](lanes/conductor39.md) for theorem work.
3. Use [Transfer matrix](concepts/transfer-matrix.md) for cross-lane context.
4. Open the relevant `sources/` dossier.
5. Pull `evidence/` or `archive/` only when exact wording or provenance is
   needed.

## Output Discipline

- Organize new work by canonical artifact, not by clock.
- Update canonical pages first; create durable `evidence/` only for theorem
  screens, clause matrices, or verdicts.
- Keep expert and literature asks lane-shaped: the unified H0/conductor-39
  support-156 78-over-78 product with `Norm_156(Y_507)` boundary; exact-P
  compact `C,D,K,orientation` packet or accepted period-156 theta2 payload.
- Start new theorem work from the
  [current theorem kernel](evidence/p25_v2_current_theorem_kernel_20260617.md);
  use the
  [Drew kernel review packet](evidence/p25_v2_drew_kernel_review_packet_20260617.md)
  for expert-facing asks and source-feedback routing;
  update lane pages only after a row produces a source-stage candidate or a
  sharp falsifier.
- Use the lightweight cockpit gate for wiki lint:
  `python3 research/p25/archive/gates/p25_v2_wiki_cockpit_lightweight_check_gate.py`.
  The [first-class-run v2 plan guard](evidence/p25_v2_first_class_run_v2_plan_guard_20260617.md)
  is included in that lint path and checks the practical fleet, first-pass
  theorem fronts, exact-P demotion, support-microscope boundary, and
  private/public mirror rule without encoding fresh heartbeat values.
  Do not run broad `p25_v2_*_gate.py` globs during production-search-first
  work; the exact-P and unified-target recomputation gates are heavy archived
  lane checks, not routine lint.
